<script setup lang="ts">import type { TraceLink } from '@/types/domain'; defineProps<{ links: TraceLink[] }>()</script>
<template><section class="trace-section"><div class="trace-section__head"><div><span class="eyebrow">CERTIFICATES</span><h3>资质与检验记录</h3></div></div><div class="cert-grid"><template v-for="link in links" :key="link.stageCode"><div v-for="certificate in link.certificates || []" :key="`${link.stageCode}-${certificate.no}`" class="cert-item"><span class="cert-item__mark">✓</span><div><strong>{{ certificate.name || '检验证明' }}</strong><small>{{ certificate.no || '已核验' }} · {{ link.enterpriseName }}</small></div></div></template><div v-if="!links.some((link) => link.certificates?.length)" class="cert-empty">当前链路未提供单独证书信息</div></div></section></template>
