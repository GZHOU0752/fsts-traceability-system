<script setup lang="ts">import type { TemperaturePoint } from '@/types/domain'; import TemperatureChart from '@/components/TemperatureChart.vue'; defineProps<{ points: TemperaturePoint[] }>()</script>
<template><section class="trace-section"><div class="trace-section__head"><div><span class="eyebrow">TEMPERATURE LOG</span><h3>冷链温度曲线</h3></div><el-tag type="success">记录完整</el-tag></div><TemperatureChart :points="points" /><div class="temperature-list"><div v-for="point in points" :key="`${point.stageCode}-${point.recordTime}`"><span>{{ point.stageName || '节点' }}</span><strong>{{ point.temperature ?? '-' }}°C</strong><small>{{ point.recordTime || '时间未记录' }}</small></div></div></section></template>
