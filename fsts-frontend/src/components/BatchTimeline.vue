<script setup lang="ts">import type { TraceLink } from '@/types/domain'; defineProps<{ links: TraceLink[] }>()</script>
<template><el-timeline class="batch-timeline"><el-timeline-item v-for="link in links" :key="`${link.stageCode}-${link.batchNo}`" :timestamp="link.handoverTime || '已记录'" placement="top" color="#20a79a"><div class="timeline-card"><div class="timeline-card__head"><strong>{{ link.stageName }}</strong><el-tag size="small" type="success">{{ link.enterpriseName }}</el-tag></div><p>{{ link.provinceName }}{{ link.cityName }} · 批号 {{ link.batchNo || '-' }}</p><span v-if="link.productVariety">{{ link.productVariety }}</span></div></el-timeline-item></el-timeline></template>
