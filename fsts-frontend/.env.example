VITE_API_BASE_URL=/api
# 二维码里承载的追溯页地址前缀，手机扫码后打开的就是它。
# 留空时：页面跑在 localhost 会自动向 /api/public/access-hosts 探测本机内网 IP，
# 生成 http://<内网IP>:<端口>/trace/<溯源码>；部署到正式域名后建议显式配置，例如
# VITE_TRACE_BASE_URL=https://trace.example.com/trace
VITE_TRACE_BASE_URL=
