# 冷冻海产品溯源系统 前后端接口文档

| 项目名称 | 冷冻海产品溯源系统（FSTS，Frozen Seafood Traceability System） |
| --- | --- |
| 文件编号 | XG1232-FSTS-API-01 |
| 版　　本 | 1.0.0-0.0.0 |
| 编制日期 | 2026-09-21 |
| 适用范围 | 系统管理端、流通节点端、消费者端（Vue3 + Element Plus + Axios + ECharts）与后端（Spring Boot + MyBatis-Plus + JDK 17） |
| 数据来源 | 《01 需求陈述书》《04 需求规格说明书》《信管1232 项目任务书》《冷冻海产品溯源系统-数据库建表脚本.sql》 |

---

## 1 文档说明

### 1.1 编写目的

本文档定义冷冻海产品溯源系统前后端之间的全部数据交互契约，作为前端页面开发、后端接口实现、前后端联调与验收测试的依据。文档中的接口路径、参数名、字段名与枚举取值必须与代码实现保持一致，任何变更需同步更新文档版本号。

### 1.2 接口设计原则

1. **三端隔离**：系统管理端、流通节点端、消费者端分别使用 `/api/admin`、`/api/enterprise`、`/api/public` 前缀，通过前缀区分访问主体，便于统一鉴权与权限配置。
2. **按企业类型自动路由**：四类节点企业的批号操作方式一致、字段不同，因此批号的新建/更新接口共用同一路径，由后端根据当前登录企业的类型（从 Token 解析）决定校验规则与入库表，前端不做类型分支请求。
3. **最小数据暴露**：流通节点端只能查看和操作本企业的数据；`enterpriseId` 等归属字段一律从 Token 解析，前端传入无效。
4. **状态由服务端判定**：批号状态流转、冷链是否合格、溯源标识码生成均在后端完成，前端只负责提交原始数据与展示结果。
5. **消费者端免登录**：消费者端接口均为匿名访问，且只返回追溯所需的公开信息（不含企业联系电话、联系人与账号等敏感字段）。

### 1.3 术语与缩写

| 术语 | 说明 |
| --- | --- |
| 节点企业 | 参与溯源链的四类企业：捕捞与养殖企业、冷冻加工企业、批发商、零售商 |
| 产品批号 | 企业在原有管理系统中生成的批次编号，在本系统中手工录入并按企业校验唯一性 |
| 上游带出 | 下游企业新建批号时，从上游企业已发布批号自动继承的数据项（产品品种、来源类型等） |
| 进场确认 | 下游企业向上游企业发起的批次交接确认请求，上游确认后下游批号方可正常使用 |
| 溯源标识码 | 零售商批号状态变为"已确认"时由系统自动生成、供消费者查询的全局唯一编码 |
| 交接温度 | 每个流通环节登记的、代表该环节冷链状态的温度值（℃） |

---

## 2 全局约定

### 2.1 请求地址

| 项 | 值 |
| --- | --- |
| 服务地址 | `http://localhost:8080` |
| 接口前缀 | `/api` |
| 数据格式 | 请求体与响应体统一为 `application/json; charset=UTF-8` |
| 字符编码 | UTF-8 |

前端开发环境在 `vue.config.js` 中配置代理，避免跨域调试问题：

```javascript
devServer: {
  port: 8081,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true
    }
  }
}
```

### 2.2 请求方法约定

| 方法 | 语义 | 使用场景 |
| --- | --- | --- |
| GET | 查询 | 列表、详情、下拉数据、统计 |
| POST | 新增 | 新建企业、新建产品批号、登录 |
| PUT | 更新 | 修改信息、状态流转（发布、下架、确认） |
| DELETE | 删除 | 删除企业、删除产品批号 |

### 2.3 请求头

| 请求头 | 是否必填 | 说明 | 示例 |
| --- | --- | --- | --- |
| `Content-Type` | POST/PUT 必填 | 请求体类型 | `application/json;charset=UTF-8` |
| `Authorization` | 需登录接口必填 | JWT 令牌，格式为 `Bearer {token}` | `Bearer eyJhbGciOiJIUzI1NiJ9...` |

### 2.4 统一响应结构

除登录失败（HTTP 401）与权限不足（HTTP 403）外，接口一律返回 HTTP 200，业务结果由 `code` 表示。

```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | Integer | 业务状态码，200 表示成功，其余见 3.6 错误码 |
| `message` | String | 提示信息，可直接用于 Element Plus 的 message 提示 |
| `data` | Object/Array/Null | 业务数据，无返回数据时为 `null` |

### 2.5 分页响应结构

分页接口的 `data` 采用 MyBatis-Plus `IPage` 结构：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "records": [],
    "total": 36,
    "size": 10,
    "current": 1,
    "pages": 4
  }
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `records` | Array | 当前页数据列表 |
| `total` | Long | 满足条件的总记录数 |
| `size` | Long | 每页条数，节点企业列表固定为 10（需求：表格最大显示 10 条数据） |
| `current` | Long | 当前页码，从 1 开始 |
| `pages` | Long | 总页数 |

### 2.6 数据格式约定

| 数据类型 | 约定 | 示例 |
| --- | --- | --- |
| 主键 ID | 后端使用 `Long`，通过 Jackson 序列化为字符串，避免 JavaScript 精度丢失 | `"1857392012345678123"` |
| 日期 | `yyyy-MM-dd` | `"2026-09-15"` |
| 日期时间 | `yyyy-MM-dd HH:mm:ss` | `"2026-09-15 07:40:00"` |
| 温度 | 数值，单位 ℃，保留 1 位小数；常规判定阈值 `-18`，速冻中心温度阈值 `-35` | `-18.5` |
| 容量 | 数值，保留 2 位小数 | `3200.00` |
| 布尔值 | `true` / `false` | `true` |
| 空值 | 使用 `null`，不使用空字符串 | `null` |

> 前端注意：涉及 ID 的入参（如 `id`、`upstreamEnterpriseId`、`upstreamBatchId`）请以字符串形式传递，避免精度丢失。

### 2.7 认证与权限

| 访问主体 | 接口前缀 | 鉴权方式 | Token 中携带的信息 |
| --- | --- | --- | --- |
| 系统管理员 | `/api/admin/**` | JWT，需管理员角色 | `userId`、`userType = ADMIN`、`loginName` |
| 节点企业 | `/api/enterprise/**` | JWT，需企业角色 | `enterpriseId`、`enterpriseType`、`enterpriseName`、`userType = ENTERPRISE` |
| 消费者 | `/api/public/**` | 免登录 | —— |
| 公共基础数据 | `/api/common/**` | 免登录 | —— |

JWT 载荷示例（节点企业）：

```json
{
  "userId": "3",
  "userType": "ENTERPRISE",
  "enterpriseId": "3",
  "enterpriseType": 3,
  "enterpriseName": "上海甬海水产批发有限公司",
  "loginName": "sh_yonghai",
  "exp": 1790000000
}
```

Token 有效期建议为 2 小时，前端在 Axios 响应拦截器中对 `code = 401` 统一跳转登录页。

### 2.8 接口清单总表

| 编号 | 模块 | 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- | --- | --- |
| 4.1 | 认证 | POST | `/api/admin/auth/login` | 系统管理员登录 | 匿名 |
| 4.2 | 认证 | GET | `/api/admin/auth/info` | 获取当前管理员信息 | 管理员 |
| 4.3 | 认证 | POST | `/api/admin/auth/logout` | 管理员退出 | 管理员 |
| 4.4 | 认证 | POST | `/api/enterprise/auth/login` | 节点企业登录 | 匿名 |
| 4.5 | 认证 | GET | `/api/enterprise/auth/info` | 获取当前登录企业信息 | 企业 |
| 4.6 | 认证 | PUT | `/api/enterprise/auth/password` | 修改本企业密码 | 企业 |
| 4.7 | 认证 | POST | `/api/enterprise/auth/logout` | 节点企业退出 | 企业 |
| 5.1 | 基础数据 | GET | `/api/common/regions/provinces` | 查询省列表 | 匿名 |
| 5.2 | 基础数据 | GET | `/api/common/regions/cities` | 按省查询市列表 | 匿名 |
| 5.3 | 基础数据 | GET | `/api/common/dicts/{typeCode}` | 查询字典项 | 匿名 |
| 6.1 | 企业注册管理 | GET | `/api/admin/enterprises` | 分页与模糊查询节点企业 | 管理员 |
| 6.2 | 企业注册管理 | GET | `/api/admin/enterprises/{id}` | 节点企业详情 | 管理员 |
| 6.3 | 企业注册管理 | POST | `/api/admin/enterprises` | 新建节点企业 | 管理员 |
| 6.4 | 企业注册管理 | PUT | `/api/admin/enterprises/{id}` | 更新节点企业 | 管理员 |
| 6.5 | 企业注册管理 | DELETE | `/api/admin/enterprises/{id}` | 删除节点企业 | 管理员 |
| 6.6 | 企业注册管理 | GET | `/api/admin/enterprises/check-name` | 企业名称或执照编号唯一性校验 | 管理员 |
| 7.1 | 数据统计 | GET | `/api/admin/statistics/overview` | 大屏顶部数据卡片 | 管理员 |
| 7.2 | 数据统计 | GET | `/api/admin/statistics/register-trend` | 十二个月注册趋势（折线图） | 管理员 |
| 7.3 | 数据统计 | GET | `/api/admin/statistics/province-distribution` | 省分组分布（饼图） | 管理员 |
| 7.4 | 数据统计 | GET | `/api/admin/statistics/province-count` | 省分组数量（柱状图） | 管理员 |
| 7.5 | 数据统计 | GET | `/api/admin/statistics/type-distribution` | 类型分组分布（饼图） | 管理员 |
| 8.1 | 企业信息 | GET | `/api/enterprise/profile` | 查询本企业信息 | 企业 |
| 9.1 | 上游选择 | GET | `/api/enterprise/upstream/enterprises` | 查询上游企业列表 | 企业 |
| 9.2 | 上游选择 | GET | `/api/enterprise/upstream/batches` | 查询上游企业已发布批号 | 企业 |
| 10.1 | 批号管理 | GET | `/api/enterprise/batches` | 分页查询本企业产品批号 | 企业 |
| 10.2 | 批号管理 | GET | `/api/enterprise/batches/{id}` | 产品批号详情 | 企业 |
| 10.3 | 批号管理 | POST | `/api/enterprise/batches` | 新建产品批号 | 企业 |
| 10.4 | 批号管理 | PUT | `/api/enterprise/batches/{id}` | 更新产品批号 | 企业 |
| 10.5 | 批号管理 | DELETE | `/api/enterprise/batches/{id}` | 删除产品批号 | 企业 |
| 10.6 | 批号管理 | PUT | `/api/enterprise/batches/{id}/publish` | 发布产品批号 | 企业 |
| 10.7 | 批号管理 | PUT | `/api/enterprise/batches/{id}/off-shelf` | 下架产品批号 | 企业 |
| 10.8 | 批号管理 | GET | `/api/enterprise/batches/check-batch-no` | 产品批号唯一性校验 | 企业 |
| 10.9 | 批号管理 | POST | `/api/enterprise/batches/{id}/confirm-request` | 向上游企业发送确认请求 | 企业（类型 2/3/4） |
| 10.10 | 批号管理 | GET | `/api/enterprise/batches/{id}/trace-code` | 查询溯源标识码 | 零售商 |
| 11.1 | 进场确认 | GET | `/api/enterprise/confirm-requests` | 待处理进场确认请求列表 | 企业（类型 2/3） |
| 11.2 | 进场确认 | GET | `/api/enterprise/confirm-requests/{id}` | 确认请求详情 | 企业 |
| 11.3 | 进场确认 | PUT | `/api/enterprise/confirm-requests/{id}/confirm` | 确认进场 | 企业（类型 1/2/3） |
| 11.4 | 进场确认 | PUT | `/api/enterprise/confirm-requests/{id}/reject` | 拒绝进场（扩展） | 企业（类型 1/2/3） |
| 12.1 | 溯源查询 | GET | `/api/public/trace/{traceCode}` | 按溯源标识码查询溯源信息 | 匿名 |

---

## 3 枚举与错误码

### 3.1 企业类型 enterpriseType

| 值 | 名称 | 说明 |
| --- | --- | --- |
| 1 | 捕捞与养殖企业 | 链条源头，无上游企业，无进场确认请求 |
| 2 | 冷冻加工企业 | 上游为捕捞与养殖企业 |
| 3 | 批发商 | 上游为冷冻加工企业，承担冷库仓储与冷藏运输 |
| 4 | 零售商 | 链条末端，上游为批发商，批号确认后生成溯源标识码 |

### 3.2 产品批号状态 batchStatus

| 值 | 名称 | 捕捞与养殖企业 | 冷冻加工/批发/零售商 | 可执行操作 |
| --- | --- | --- | --- | --- |
| 1 | 新建（待发布） | 是 | 是 | 更新、删除、发布；类型 2/3/4 可发送确认请求 |
| 2 | 待确认 | 不使用 | 是 | 仅可查看，等待上游确认 |
| 3 | 已确认（已发布） | 是 | 是 | 可被下游选择；可下架；零售商此时生成溯源标识码 |
| 4 | 已下架 | 是 | 是 | 不可浏览、不可被下游选择 |

> 前端列表页按状态用单选按钮分组查询，`batchStatus = 4` 的记录由后端直接过滤，不在列表中返回（需求：已下架状态产品批号不能浏览）。

### 3.3 进场确认请求状态 requestStatus

| 值 | 名称 | 说明 |
| --- | --- | --- |
| 1 | 待确认 | 下游已发起，等待上游处理 |
| 2 | 已确认 | 上游确认通过，下游批号状态同步置 3 |
| 3 | 已拒绝 | 上游拒绝，下游批号回退为"新建"状态 |
| 4 | 已撤回 | 下游在确认前撤回请求（扩展功能） |

### 3.4 其他枚举

| 枚举 | 取值 |
| --- | --- |
| 产品来源类型 `sourceType` | 1 养殖，2 捕捞 |
| 来源证明材料类型 `certificateType` | 1 产地检疫合格证明，2 渔获物上岸证明 |
| 加工形态 `processForm` | 整鱼、去头、鱼片、鱼段、鱼块、鱼柳、虾仁、其他（业务库中存中文名称，候选值由字典 `process_form` 提供） |
| 企业状态 `status` | 1 正常，0 停用 |
| 溯源标识码状态 `status` | 1 有效，0 失效 |

### 3.5 溯源标识码编码规则

格式：`FSTS-yyyyMMdd-{省简称拼音首字母}-{4 位流水号}`

示例：`FSTS-20260915-SH-0001`（2026-09-15 生成、上海市零售商、当日第 1 个）

规则要点：全局唯一（数据库 `trace_code.trace_code` 唯一索引）；一个零售商产品批号只会生成一个标识码；批号下架后标识码状态同步置 0，消费者查询时返回"该标识码已失效"。

### 3.6 错误码

| code | 含义 | 触发场景举例 | 前端处理建议 |
| --- | --- | --- | --- |
| 200 | 成功 | —— | 正常处理 data |
| 400 | 参数校验失败 | 必填项为空、温度格式非法 | message 提示，定位到表单字段 |
| 401 | 未登录或登录已失效 | Token 缺失、过期 | 清空本地 Token，跳转登录页 |
| 403 | 权限不足 | 企业访问管理端接口、访问他企业数据 | 提示"无操作权限" |
| 404 | 资源不存在 | 批号 ID、企业 ID 不存在 | 提示并刷新列表 |
| 409 | 状态冲突 | 对已确认批号执行更新、重复发起确认请求 | message 提示当前状态不允许该操作 |
| 1001 | 账号或密码错误 | 登录失败 | 提示重新输入 |
| 1002 | 账号已停用 | 管理员停用企业后登录 | 提示联系系统管理员 |
| 2001 | 产品批号已存在 | 同企业内批号重复（输入框 blur 校验） | 表单字段标红提示 |
| 2002 | 企业名称、登录账号或营业执照编号已存在 | 管理端新建或编辑企业 | 表单字段标红提示 |
| 2003 | 上游产品批号不存在或未发布 | 下游选择了已下架或未发布的上游批号 | 重新选择上游批号 |
| 2004 | 温度不符合冷链要求 | 温度高于 -18 ℃（加工环节速冻中心温度高于 -35 ℃） | 弹窗警告并要求二次确认，确认后仍可提交但标记为冷链异常 |
| 2005 | 批号状态不允许该操作 | 删除已发布批号、更新待确认批号 | 提示并刷新列表 |
| 3001 | 溯源标识码不存在 | 消费者输入错误编码 | 提示"未查询到该溯源标识码" |
| 3002 | 溯源标识码已失效 | 对应批号已下架 | 提示"该批次商品已停止追溯服务" |
| 500 | 系统异常 | 数据库异常等 | 提示"系统繁忙，请稍后重试" |

---

## 4 认证模块

### 4.1 系统管理员登录

**接口说明**：系统管理端登录，对应需求规格说明书 4.2 登录用例。登录成功后方可对节点企业注册信息管理与可视化大屏进行操作。

| 项 | 内容 |
| --- | --- |
| 请求方式 | POST |
| 请求路径 | `/api/admin/auth/login` |
| 权限 | 匿名 |

**请求参数（Body）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `loginName` | String | 是 | 登录账号，1~50 字符 | `admin` |
| `password` | String | 是 | 登录密码，前端以明文提交，后端 BCrypt 校验 | `123456` |

**请求示例**

```json
{
  "loginName": "admin",
  "password": "123456"
}
```

**响应示例**

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyVHlwZSI6IkFETUlOIn0.xxx",
    "userId": "1",
    "userType": "ADMIN",
    "loginName": "admin",
    "adminName": "系统管理员",
    "expiresIn": 7200
  }
}
```

**业务规则**

1. 账号不存在或密码错误统一返回 `code = 1001`，不区分提示，防止账号枚举。
2. `status = 0` 的管理员登录时返回 `code = 1002`。
3. 登录成功后更新 `sys_admin.last_login_time`。
4. `password` 字段禁止写入日志。

---

### 4.2 获取当前管理员信息

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/auth/info` |
| 权限 | 管理员 |

**请求参数**：无（身份从 Token 解析）

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "userId": "1",
    "loginName": "admin",
    "adminName": "系统管理员",
    "phone": "13800000000",
    "email": null,
    "lastLoginTime": "2026-09-21 09:05:12"
  }
}
```

---

### 4.3 管理员退出登录

| 项 | 内容 |
| --- | --- |
| 请求方式 | POST |
| 请求路径 | `/api/admin/auth/logout` |
| 权限 | 管理员 |

**请求参数**：无

**响应示例**

```json
{
  "code": 200,
  "message": "退出成功",
  "data": null
}
```

> 若采用无状态 JWT，可将 Token 加入黑名单（Redis 或本地缓存，缓存时长等于 Token 剩余有效期）；简化实现时前端删除本地 Token 即可。

---

### 4.4 节点企业登录

**接口说明**：四类节点企业共用一个登录入口，登录成功后前端根据 `enterpriseType` 渲染对应的菜单与批号表单。

| 项 | 内容 |
| --- | --- |
| 请求方式 | POST |
| 请求路径 | `/api/enterprise/auth/login` |
| 权限 | 匿名 |

**请求参数（Body）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `loginName` | String | 是 | 登录账号 | `sh_yonghai` |
| `password` | String | 是 | 登录密码 | `123456` |

**请求示例**

```json
{
  "loginName": "sh_yonghai",
  "password": "123456"
}
```

**响应示例**

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9.eyJ1c2VyVHlwZSI6IkVOVEVSUFJJU0UifQ.xxx",
    "enterpriseId": "3",
    "enterpriseCode": "FSTS-E-20250003",
    "enterpriseName": "上海甬海水产批发有限公司",
    "enterpriseType": 3,
    "enterpriseTypeName": "批发商",
    "provinceName": "上海市",
    "cityName": "上海市",
    "expiresIn": 7200
  }
}
```

**业务规则**

1. 未在系统中注册的企业不能登录（本系统不提供企业自助注册入口，注册只能通过系统管理端完成）。
2. 企业状态为停用（`status = 0`）时返回 `code = 1002`。
3. 登录成功后更新 `node_enterprise.last_login_time`。
4. 前端依据 `enterpriseType` 决定：是否展示"向上游企业发送确认请求"（类型 1 不展示）、是否展示"下游企业进场确认"（类型 4 不展示）、批发类型表单是否包含三个温度字段、零售类型是否展示溯源标识码入口。

---

### 4.5 获取当前登录企业信息

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/auth/info` |
| 权限 | 企业 |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "enterpriseId": "3",
    "enterpriseCode": "FSTS-E-20250003",
    "enterpriseName": "上海甬海水产批发有限公司",
    "enterpriseType": 3,
    "enterpriseTypeName": "批发商",
    "loginName": "sh_yonghai",
    "creditCode": "91310115MA1H3M8P7C",
    "legalPerson": "陈建国",
    "contactPerson": "周敏",
    "contactPhone": "13817800003",
    "provinceCode": "310000",
    "provinceName": "上海市",
    "cityCode": "310100",
    "cityName": "上海市",
    "address": "上海市浦东新区外高桥保税区洲海路 289 号 2 号冷库",
    "foodBusinessLicenseNo": "JY13101150028641",
    "roadTransportLicenseNo": "沪交运管许可 310115002863",
    "coldStorageCapacity": 3200.00,
    "transportToolInfo": "冷藏车 6 辆（沪B-LC0231 等）",
    "displayEquipmentInfo": null,
    "registerTime": "2025-11-05 10:20:00",
    "lastLoginTime": "2026-09-21 09:10:33"
  }
}
```

> 返回字段随企业类型不同而变化，不属于该类型的资质字段返回 `null`。

---

### 4.6 修改本企业密码

**接口说明**：需求规格说明书 1.5.2 明确"企业注册信息管理：只能对注册信息中的密码进行更新"，因此流通节点端不提供其他注册信息的修改接口。

| 项 | 内容 |
| --- | --- |
| 请求方式 | PUT |
| 请求路径 | `/api/enterprise/auth/password` |
| 权限 | 企业 |

**请求参数（Body）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `oldPassword` | String | 是 | 原密码 | `123456` |
| `newPassword` | String | 是 | 新密码，长度 6~20 | `Fsts@2026` |
| `confirmPassword` | String | 是 | 确认新密码，须与 `newPassword` 一致 | `Fsts@2026` |

**请求示例**

```json
{
  "oldPassword": "123456",
  "newPassword": "Fsts@2026",
  "confirmPassword": "Fsts@2026"
}
```

**响应示例**

```json
{
  "code": 200,
  "message": "密码修改成功，请重新登录",
  "data": null
}
```

**业务规则**：原密码错误返回 `code = 400`，提示"原密码不正确"；两次新密码输入不一致由前端校验，后端同样校验一次；修改成功后 Token 失效，前端跳转登录页。

---

### 4.7 节点企业退出登录

| 项 | 内容 |
| --- | --- |
| 请求方式 | POST |
| 请求路径 | `/api/enterprise/auth/logout` |
| 权限 | 企业 |

**响应示例**

```json
{
  "code": 200,
  "message": "退出成功",
  "data": null
}
```

---

## 5 公共基础数据接口

### 5.1 查询省列表

**接口说明**：用于各页面"省下拉列表"初始化，对应需求规格说明书 4.1、5.2.1、5.3.1、5.4.1、5.5.1 中"初始化时查询省信息"。默认不设置初始选中项。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/common/regions/provinces` |
| 权限 | 匿名 |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    { "regionCode": "210000", "regionName": "辽宁省", "shortName": "辽宁", "sortNo": 6 },
    { "regionCode": "310000", "regionName": "上海市", "shortName": "上海", "sortNo": 9 },
    { "regionCode": "370000", "regionName": "山东省", "shortName": "山东", "sortNo": 15 }
  ]
}
```

### 5.2 按省查询市列表

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/common/regions/cities` |
| 权限 | 匿名 |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `provinceCode` | String | 是 | 省行政区划代码 | `370000` |

**请求示例**

```
GET /api/common/regions/cities?provinceCode=370000
```

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    { "regionCode": "370100", "regionName": "济南市", "sortNo": 1 },
    { "regionCode": "370200", "regionName": "青岛市", "sortNo": 2 },
    { "regionCode": "370600", "regionName": "烟台市", "sortNo": 6 }
  ]
}
```

> 前端交互：省下拉列表 `change` 事件触发本接口，填充市下拉列表并清空原选中值。

### 5.3 查询字典项

**接口说明**：统一提供企业类型、来源类型、加工形态、产品品种、批号状态等下拉候选数据。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/common/dicts/{typeCode}` |
| 权限 | 匿名 |

**路径参数**

| 参数名 | 类型 | 必填 | 说明 | 可选值 |
| --- | --- | --- | --- | --- |
| `typeCode` | String | 是 | 字典类型编码 | `enterprise_type`、`source_type`、`certificate_type`、`process_form`、`product_variety`、`batch_status`、`request_status` |

**请求示例**

```
GET /api/common/dicts/product_variety
```

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    { "itemCode": "ChangYu", "itemValue": "鲳鱼", "sortNo": 4 },
    { "itemCode": "DaiYu", "itemValue": "带鱼", "sortNo": 1 },
    { "itemCode": "YouYu", "itemValue": "鱿鱼", "sortNo": 11 }
  ]
}
```

**业务规则**：只返回 `status = 1` 的字典项，按 `sortNo` 升序；`process_form`、`product_variety` 类型的业务值使用字典项的 `itemValue`（中文名称）存储，例如 `processForm = "鱼片"`、`productVariety = "带鱼"`。

---

## 6 系统管理端——节点企业注册信息管理

本章对应需求规格说明书 4.1 节点企业注册信息管理用例及事件描述表。所有接口仅系统管理员可访问，流通节点端不提供企业注册信息的增删改接口。

### 6.1 分页查询节点企业列表

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/enterprises` |
| 权限 | 管理员 |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `enterpriseName` | String | 否 | 企业名称，模糊匹配 | `水产` |
| `creditCode` | String | 否 | 统一社会信用代码或营业执照编号，模糊匹配 | `91310115` |
| `provinceCode` | String | 否 | 所在省代码，精确匹配 | `310000` |
| `cityCode` | String | 否 | 所在市代码，精确匹配 | `310100` |
| `enterpriseType` | Integer | 否 | 企业类型：1/2/3/4 | `3` |
| `status` | Integer | 否 | 状态：1 正常，0 停用 | `1` |
| `current` | Long | 否 | 当前页码，默认 1 | `1` |
| `size` | Long | 否 | 每页条数，默认 10 | `10` |

**请求示例**

```
GET /api/admin/enterprises?enterpriseName=水产&provinceCode=310000&current=1&size=10
```

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "records": [
      {
        "id": "3",
        "enterpriseCode": "FSTS-E-20250003",
        "enterpriseName": "上海甬海水产批发有限公司",
        "enterpriseType": 3,
        "enterpriseTypeName": "批发商",
        "creditCode": "91310115MA1H3M8P7C",
        "legalPerson": "陈建国",
        "contactPerson": "周敏",
        "contactPhone": "13817800003",
        "provinceCode": "310000",
        "provinceName": "上海市",
        "cityCode": "310100",
        "cityName": "上海市",
        "address": "上海市浦东新区外高桥保税区洲海路 289 号 2 号冷库",
        "status": 1,
        "statusName": "正常",
        "registerTime": "2025-11-05 10:20:00"
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

**业务规则**

1. 查询条件全部为空时返回全部企业（逻辑删除记录除外）。
2. 列表不返回企业资质证件明细，避免列表接口数据过重，资质信息通过详情接口获取。
3. 模糊查询作用于 `enterpriseName`、`creditCode`；`provinceCode`、`cityCode`、`enterpriseType`、`status` 为精确筛选。
4. "清空"按钮由前端重置查询表单后重新调用本接口（不带查询条件）。

---

### 6.2 查询节点企业详情

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/enterprises/{id}` |
| 权限 | 管理员 |

**路径参数**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | String | 是 | 企业 ID |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": "1",
    "enterpriseCode": "FSTS-E-20250001",
    "enterpriseName": "青岛远洋渔业有限公司",
    "enterpriseType": 1,
    "enterpriseTypeName": "捕捞与养殖企业",
    "loginName": "qd_yuanye",
    "creditCode": "91370200MA3D1K2X8A",
    "legalPerson": "王海生",
    "contactPerson": "刘志强",
    "contactPhone": "13805320001",
    "provinceCode": "370000",
    "provinceName": "山东省",
    "cityCode": "370200",
    "cityName": "青岛市",
    "address": "青岛市市南区香港中路 88 号远洋大厦 12 层",
    "fisheryLicenseNo": "LYQZ-2025-3702-0071",
    "aquacultureLicenseNo": null,
    "fryLicenseNo": null,
    "foodProductionLicenseNo": null,
    "exportFilingNo": null,
    "foodBusinessLicenseNo": null,
    "roadTransportLicenseNo": null,
    "coldStorageCapacity": null,
    "transportToolInfo": null,
    "displayEquipmentInfo": null,
    "status": 1,
    "statusName": "正常",
    "registerTime": "2025-10-08 09:12:00",
    "lastLoginTime": "2026-09-21 08:40:11",
    "createTime": "2025-10-08 09:12:00",
    "updateTime": "2026-09-21 08:40:11"
  }
}
```

**业务规则**：记录不存在或已逻辑删除时返回 `code = 404`；响应中不返回 `password` 字段。

---

### 6.3 新建节点企业

**接口说明**：对应事件描述"新建按钮｜Click｜弹出新建对话框，完成注册操作；企业类型下拉列表 Change 时切换资质证件填写项"。

| 项 | 内容 |
| --- | --- |
| 请求方式 | POST |
| 请求路径 | `/api/admin/enterprises` |
| 权限 | 管理员 |

**请求参数（Body）——公共字段**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `enterpriseName` | String | 是 | 企业名称，1~100 字符 | `青岛远洋渔业有限公司` |
| `enterpriseType` | Integer | 是 | 企业类型 1/2/3/4 | `1` |
| `loginName` | String | 是 | 登录账号，1~50 字符，全局唯一 | `qd_yuanye` |
| `password` | String | 否 | 初始密码，不传则默认 `123456` | `123456` |
| `creditCode` | String | 是 | 统一社会信用代码或营业执照编号，全局唯一 | `91370200MA3D1K2X8A` |
| `legalPerson` | String | 否 | 法定代表人 | `王海生` |
| `contactPerson` | String | 否 | 联系人 | `刘志强` |
| `contactPhone` | String | 否 | 联系电话 | `13805320001` |
| `provinceCode` | String | 是 | 所在省代码 | `370000` |
| `cityCode` | String | 是 | 所在市代码 | `370200` |
| `address` | String | 否 | 详细地址 | `青岛市市南区香港中路 88 号` |
| `status` | Integer | 否 | 状态，默认 1 | `1` |

**按企业类型附加的资质字段**

| 企业类型 | 附加字段 | 必填要求 | 说明 |
| --- | --- | --- | --- |
| 1 捕捞与养殖企业 | `fisheryLicenseNo` | 海洋捕捞必填 | 渔业捕捞许可证编号 |
| | `aquacultureLicenseNo` | 水产养殖必填 | 水域滩涂养殖证编号 |
| | `fryLicenseNo` | 否 | 水产苗种生产许可证编号（苗种繁育） |
| 2 冷冻加工企业 | `foodProductionLicenseNo` | 是 | 食品生产许可证编号 |
| | `exportFilingNo` | 否 | 出口食品生产企业备案编号（出口产品） |
| 3 批发商 | `foodBusinessLicenseNo` | 是 | 食品经营许可证编号 |
| | `roadTransportLicenseNo` | 否 | 道路运输经营许可证编号（自营冷藏运输） |
| | `coldStorageCapacity` | 否 | 冷库容量（吨），数值 |
| | `transportToolInfo` | 否 | 冷藏运输工具信息 |
| 4 零售商 | `foodBusinessLicenseNo` | 是 | 食品经营许可证编号 |
| | `displayEquipmentInfo` | 否 | 冷冻陈列设备信息 |

> 类型 1 的 `fisheryLicenseNo` 与 `aquacultureLicenseNo` 至少填写一项，允许同时填写（兼营捕捞与养殖）。

**请求示例（新建捕捞与养殖企业）**

```json
{
  "enterpriseName": "青岛远洋渔业有限公司",
  "enterpriseType": 1,
  "loginName": "qd_yuanye",
  "creditCode": "91370200MA3D1K2X8A",
  "legalPerson": "王海生",
  "contactPerson": "刘志强",
  "contactPhone": "13805320001",
  "provinceCode": "370000",
  "cityCode": "370200",
  "address": "青岛市市南区香港中路 88 号远洋大厦 12 层",
  "fisheryLicenseNo": "LYQZ-2025-3702-0071",
  "aquacultureLicenseNo": null,
  "fryLicenseNo": null,
  "status": 1
}
```

**请求示例（新建批发商）**

```json
{
  "enterpriseName": "上海甬海水产批发有限公司",
  "enterpriseType": 3,
  "loginName": "sh_yonghai",
  "creditCode": "91310115MA1H3M8P7C",
  "contactPerson": "周敏",
  "contactPhone": "13817800003",
  "provinceCode": "310000",
  "cityCode": "310100",
  "address": "上海市浦东新区外高桥保税区洲海路 289 号 2 号冷库",
  "foodBusinessLicenseNo": "JY13101150028641",
  "roadTransportLicenseNo": "沪交运管许可 310115002863",
  "coldStorageCapacity": 3200.00,
  "transportToolInfo": "冷藏车 6 辆（沪B-LC0231 等）",
  "status": 1
}
```

**响应示例**

```json
{
  "code": 200,
  "message": "新建成功",
  "data": {
    "id": "17",
    "enterpriseCode": "FSTS-E-20260017",
    "loginName": "qd_yuanye",
    "initialPassword": "123456"
  }
}
```

**业务规则**

1. `enterpriseCode` 由后端按 `FSTS-E-{注册年份}{4 位流水}` 规则生成，不接受前端传入。
2. `provinceName`、`cityName` 由后端根据 `provinceCode`、`cityCode` 查询 `sys_region` 回填，前端只传代码。
3. 登录账号、统一社会信用代码重复时返回 `code = 2002`。
4. 企业类型与资质字段的对应关系由后端校验：传入了不属于该类型的资质字段时忽略且不报错。
5. 初始密码以 BCrypt 加密存储；未传 `password` 时使用默认密码 `123456` 并在响应中回显，便于管理员告知企业。
6. `register_time` 取当前时间，用于可视化大屏的注册趋势统计。

---

### 6.4 更新节点企业

| 项 | 内容 |
| --- | --- |
| 请求方式 | PUT |
| 请求路径 | `/api/admin/enterprises/{id}` |
| 权限 | 管理员 |

**请求参数**：同 6.3，另需注意以下差异

| 参数名 | 说明 |
| --- | --- |
| `id` | 路径参数，必填 |
| `password` | 本接口不接收。密码更新由流通节点端企业自行完成（需求规格说明书 4.1 明确） |
| `loginName` | 允许修改，需保持全局唯一 |
| `enterpriseType` | 不允许修改（已注册企业变更类型会破坏溯源链），传入则返回 `code = 400` |

**请求示例**

```
PUT /api/admin/enterprises/2
```

```json
{
  "enterpriseName": "烟台海珍冷冻食品有限公司",
  "creditCode": "91370600MA3D2L4Y6B",
  "contactPerson": "张丽",
  "contactPhone": "13805350002",
  "provinceCode": "370000",
  "cityCode": "370600",
  "address": "烟台市芝罘区幸福南路 156 号冷链产业园 3 号厂房",
  "foodProductionLicenseNo": "SC11337060200136",
  "exportFilingNo": "BA2025-3706-0032",
  "status": 1
}
```

**响应示例**

```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

### 6.5 删除节点企业

| 项 | 内容 |
| --- | --- |
| 请求方式 | DELETE |
| 请求路径 | `/api/admin/enterprises/{id}` |
| 权限 | 管理员 |

**响应示例**

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

**业务规则**

1. 采用逻辑删除（`deleted = 1`），保证已发生的溯源链数据不被破坏。
2. 企业存在产品批号记录时，仅允许逻辑删除并同时停用账号，其历史批次在消费者端溯源查询中仍可正常展示。
3. 删除为不可恢复操作，前端需二次确认（`ElMessageBox.confirm`）。

---

### 6.6 企业名称与营业执照编号唯一性校验

**接口说明**：用于新建/编辑对话框中对应输入框的 `blur` 事件校验，避免提交后才报错。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/enterprises/check-name` |
| 权限 | 管理员 |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `field` | String | 是 | 校验字段：`loginName`、`creditCode` | `creditCode` |
| `value` | String | 是 | 待校验的值 | `91370600MA3D2L4Y6B` |
| `excludeId` | String | 否 | 编辑场景下排除自身 ID | `2` |

**响应示例**

```json
{
  "code": 200,
  "message": "校验完成",
  "data": { "available": true }
}
```

> `available = true` 表示可用；`false` 表示已存在，前端在表单中提示"该统一社会信用代码已注册"。

---

## 7 系统管理端——数据统计（可视化大屏）

本章对应需求规格说明书 4.3 节点企业注册信息统计用例及其事件描述表。大屏页面初始化时并行调用 7.1 至 7.5 五个接口；其中 7.3 与 7.4 数据源相同，前端可复用同一份数据分别渲染饼图与柱状图，减少一次请求。

### 7.1 大屏概览数据卡片

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/statistics/overview` |
| 权限 | 管理员 |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "totalCount": 16,
    "fishingCount": 5,
    "processingCount": 3,
    "wholesaleCount": 5,
    "retailCount": 3,
    "provinceCount": 8,
    "todayRegisterCount": 0,
    "updateTime": "2026-09-21 09:20:00"
  }
}
```

| 字段 | 说明 |
| --- | --- |
| `totalCount` | 注册节点企业总数 |
| `fishingCount` / `processingCount` / `wholesaleCount` / `retailCount` | 四类企业数量 |
| `provinceCount` | 已覆盖的省份数量 |
| `todayRegisterCount` | 当日新增注册企业数量 |
| `updateTime` | 统计时间，用于大屏右上角展示 |

### 7.2 十二个月内注册数量趋势（折线图）

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/statistics/register-trend` |
| 权限 | 管理员 |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `months` | Integer | 否 | 统计月数，默认 12（含当前月及其之前 11 个月） | `12` |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "months": ["2025-10", "2025-11", "2025-12", "2026-01", "2026-02", "2026-03",
               "2026-04", "2026-05", "2026-06", "2026-07", "2026-08", "2026-09"],
    "counts": [2, 2, 2, 1, 1, 2, 1, 1, 1, 1, 1, 1],
    "total": 16
  }
}
```

**业务规则**：无注册记录的月份必须补 0（由服务层生成连续 12 个月的坐标轴，不能直接用数据库分组结果充当横轴），否则折线图横轴断裂。SQL 参考见附录 C.2。

### 7.3 省分组注册数量分布（饼图）

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/statistics/province-distribution` |
| 权限 | 管理员 |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `top` | Integer | 否 | 只返回数量最多的前 N 个省（饼图避免图例过多）；不传返回全部 | `10` |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    { "provinceCode": "370000", "provinceName": "山东省", "count": 4, "percent": 25.00 },
    { "provinceCode": "440000", "provinceName": "广东省", "count": 3, "percent": 18.75 },
    { "provinceCode": "310000", "provinceName": "上海市", "count": 2, "percent": 12.50 }
  ]
}
```

### 7.4 省分组注册数量统计（柱状图）

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/statistics/province-count` |
| 权限 | 管理员 |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "provinces": ["山东省", "广东省", "上海市", "福建省", "浙江省", "辽宁省", "江苏省", "海南省"],
    "counts": [4, 3, 2, 2, 2, 1, 1, 1]
  }
}
```

> 与 7.3 的差异：本接口按数量降序返回全部省份，直接给出 ECharts 柱状图所需的 `xAxis.data` 与 `series.data`；7.3 额外返回占比字段 `percent` 用于饼图 tooltip 展示，并可截取前 N 名。

### 7.5 企业类型分组注册数量分布（饼图）

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/admin/statistics/type-distribution` |
| 权限 | 管理员 |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    { "enterpriseType": 1, "enterpriseTypeName": "捕捞与养殖企业", "count": 5, "percent": 31.25 },
    { "enterpriseType": 2, "enterpriseTypeName": "冷冻加工企业", "count": 3, "percent": 18.75 },
    { "enterpriseType": 3, "enterpriseTypeName": "批发商", "count": 5, "percent": 31.25 },
    { "enterpriseType": 4, "enterpriseTypeName": "零售商", "count": 3, "percent": 18.75 }
  ]
}
```

---

## 8 流通节点端——本企业信息

### 8.1 查询本企业信息

**接口说明**：用于"企业注册信息管理"页面展示本企业注册信息。与 4.5 同源，返回结构一致，前端可复用同一个数据模型。按需求，本页面只能修改密码（接口 4.6），其余字段只读。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/profile` |
| 权限 | 企业 |

**请求参数**：无

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "enterpriseId": "4",
    "enterpriseCode": "FSTS-E-20250004",
    "enterpriseName": "上海佳鲜生鲜超市有限公司",
    "enterpriseType": 4,
    "enterpriseTypeName": "零售商",
    "creditCode": "91310115MA1H4N9Q8D",
    "legalPerson": "赵晓燕",
    "contactPerson": "孙涛",
    "contactPhone": "13817800004",
    "provinceName": "上海市",
    "cityName": "上海市",
    "address": "上海市浦东新区世纪大道 100 号佳鲜生鲜超市陆家嘴店",
    "foodBusinessLicenseNo": "JY13101150031972",
    "displayEquipmentInfo": "冷冻岛柜 8 台（LG-01 至 LG-08，单体温度 -20 ℃）",
    "registerTime": "2025-11-19 16:48:00",
    "editableFields": ["password"]
  }
}
```

> `editableFields` 用于前端统一控制可编辑字段，本系统固定为 `["password"]`。

---

## 9 流通节点端——上游数据选择

### 9.1 查询上游企业列表

**接口说明**：对应事件描述"市下拉列表｜Change｜根据选中的省和市查询上游企业信息列表，并填充到上游企业名称下拉列表中"。上游企业类型由当前登录企业类型自动推定，前端无需传企业类型。

| 当前企业类型 | 自动匹配的上游企业类型 | 对应表 1-1 的流通顺序 |
| --- | --- | --- |
| 1 捕捞与养殖企业 | —— | 链条源头，调用本接口返回 `code = 400` |
| 2 冷冻加工企业 | 1 捕捞与养殖企业 | 捕捞与养殖 → 冷冻加工 |
| 3 批发商 | 2 冷冻加工企业 | 冷冻加工 → 批发 |
| 4 零售商 | 3 批发商 | 批发 → 零售 |

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/upstream/enterprises` |
| 权限 | 企业（类型 2/3/4） |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `provinceCode` | String | 否 | 上游企业所在省代码 | `370000` |
| `cityCode` | String | 否 | 上游企业所在市代码 | `370600` |
| `enterpriseName` | String | 否 | 上游企业名称，模糊匹配 | `海珍` |
| `current` | Long | 否 | 当前页码，默认 1；下拉远程搜索场景可传 `size = 50` | `1` |
| `size` | Long | 否 | 每页条数，默认 10 | `50` |

**请求示例**

```
GET /api/enterprise/upstream/enterprises?provinceCode=370000&cityCode=370600&size=50
```

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "records": [
      {
        "id": "2",
        "enterpriseName": "烟台海珍冷冻食品有限公司",
        "enterpriseType": 2,
        "enterpriseTypeName": "冷冻加工企业",
        "provinceCode": "370000",
        "provinceName": "山东省",
        "cityCode": "370600",
        "cityName": "烟台市",
        "address": "烟台市芝罘区幸福南路 156 号冷链产业园 3 号厂房"
      }
    ],
    "total": 1,
    "size": 50,
    "current": 1,
    "pages": 1
  }
}
```

**业务规则**：只返回 `status = 1` 且未被逻辑删除的上游企业；返回结果不包含上游企业的联系人与联系电话（非必要信息）。

---

### 9.2 查询上游企业已发布产品批号

**接口说明**：对应事件描述"上游企业名称下拉列表｜Change｜查询该企业处于已发布状态的产品批号列表，并填充到上游企业产品批号下拉列表中"和"上游企业产品批号下拉列表｜Change｜将选中批号的产品品种填充到产品品种输入框中，产品品种输入框禁用"。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/upstream/batches` |
| 权限 | 企业（类型 2/3/4） |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `upstreamEnterpriseId` | String | 是 | 上游企业 ID | `2` |
| `batchNo` | String | 否 | 上游产品批号，模糊匹配 | `JG2026` |
| `current` | Long | 否 | 当前页码，默认 1 | `1` |
| `size` | Long | 否 | 每页条数，默认 10 | `50` |

**请求示例**

```
GET /api/enterprise/upstream/batches?upstreamEnterpriseId=2&size=50
```

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "records": [
      {
        "id": "2",
        "batchNo": "JG20260905001",
        "productVariety": "带鱼",
        "sourceType": 2,
        "sourceTypeName": "捕捞",
        "enterpriseName": "烟台海珍冷冻食品有限公司",
        "publishTime": "2026-09-05 16:20:00",
        "handoverTemp": -19.00,
        "coldChainOk": true
      }
    ],
    "total": 1,
    "size": 50,
    "current": 1,
    "pages": 1
  }
}
```

**业务规则**

1. 只返回 `batchStatus = 3`（已确认/已发布）的批号；已下架（4）与新建（1）、待确认（2）的批号不可被下游选择。
2. 返回的 `productVariety`、`sourceType` 即"上游带出"数据项，下游新建批号时原样提交，前端将品种输入框设为禁用。
3. 若上游批号在提交时已被下架，后端返回 `code = 2003`。

---

## 10 流通节点端——产品批号管理

### 10.1 分页查询本企业产品批号列表

**接口说明**：对应各企业"浏览产品批号列表用例"。列表只返回当前登录企业自己的批号；前端按状态用单选按钮分组（新建、待确认、已确认/已发布），已下架批号不返回。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/batches` |
| 权限 | 企业 |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `batchStatus` | Integer | 是 | 状态分组：1 新建、2 待确认、3 已确认/已发布 | `1` |
| `batchNo` | String | 否 | 产品批号，模糊匹配 | `JG2026` |
| `productVariety` | String | 否 | 产品品种，精确匹配 | `带鱼` |
| `upstreamBatchNo` | String | 否 | 上游（进场）产品批号，模糊匹配 | `YY2026` |
| `current` | Long | 否 | 当前页码，默认 1 | `1` |
| `size` | Long | 否 | 每页条数，默认 10 | `10` |

**请求示例**

```
GET /api/enterprise/batches?batchStatus=1&current=1&size=10
```

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "records": [
      {
        "id": "7",
        "batchNo": "JG20260920001",
        "productVariety": "带鱼",
        "upstreamBatchNo": "YY20260901001",
        "upstreamEnterpriseName": "青岛远洋渔业有限公司",
        "batchStatus": 1,
        "batchStatusName": "新建",
        "handoverTemp": -19.20,
        "coldChainOk": true,
        "createTime": "2026-09-20 10:05:00"
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

**列表按钮显示规则（前端）**

| 状态 | 行内按钮 |
| --- | --- |
| 1 新建 | `更新`、`删除` |
| 2 待确认 | 无（只可查看，不能修改） |
| 3 已确认 / 已发布 | `下架` |
| 4 已下架 | 后端不返回该状态数据 |

### 10.2 查询产品批号详情

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/batches/{id}` |
| 权限 | 企业（只能查询本企业批号） |

**路径参数**

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | String | 是 | 产品批号 ID |

**响应示例（零售商批号，已确认并已生成溯源标识码）**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": "4",
    "batchNo": "LS20260915001",
    "enterpriseId": "4",
    "enterpriseName": "上海佳鲜生鲜超市有限公司",
    "enterpriseType": 4,
    "enterpriseTypeName": "零售商",
    "upstreamEnterpriseId": "3",
    "upstreamEnterpriseName": "上海甬海水产批发有限公司",
    "upstreamProvinceCode": "310000",
    "upstreamProvinceName": "上海市",
    "upstreamCityCode": "310100",
    "upstreamCityName": "上海市",
    "upstreamBatchId": "3",
    "upstreamBatchNo": "PF20260910001",
    "productVariety": "带鱼",
    "sourceType": 2,
    "sourceTypeName": "捕捞",
    "batchStatus": 3,
    "batchStatusName": "已确认",
    "handoverTemp": -18.20,
    "coldChainOk": true,
    "publishTime": "2026-09-15 07:40:00",
    "offShelfTime": null,
    "remark": "陆家嘴店上架销售，柜台陈列温度 -18.2 ℃",
    "createTime": "2026-09-15 07:20:00",
    "updateTime": "2026-09-15 07:40:00",
    "retailInfo": {
      "shelfDate": "2026-09-15",
      "displayTemp": -18.20,
      "saleStore": "佳鲜生鲜超市 陆家嘴店",
      "displayEquipNo": "LG-03"
    },
    "traceCode": {
      "traceCode": "FSTS-20260915-SH-0001",
      "status": 1,
      "statusName": "有效",
      "generateTime": "2026-09-15 07:36:00",
      "qrContent": "https://fsts.example.com/trace/FSTS-20260915-SH-0001",
      "queryCount": 12
    },
    "confirmRequest": {
      "requestNo": "FSTS-CR-20260914-0001",
      "requestStatus": 2,
      "requestStatusName": "已确认",
      "requestTime": "2026-09-14 18:20:00",
      "handleTime": "2026-09-15 07:35:00",
      "handleRemark": "实收数量与温度记录相符，同意确认"
    }
  }
}
```

**明细对象按企业类型返回，字段如下**

| 企业类型 | 明细对象 | 字段 |
| --- | --- | --- |
| 1 捕捞与养殖企业 | `fishingInfo` | `catchBreedDate`、`certificateType`、`certificateTypeName`、`certificateNo`、`departureTemp`、`drugReportNo`、`fishingLogNo` |
| 2 冷冻加工企业 | `processingInfo` | `processForm`、`inspectionNo`、`quickFreezeTemp`、`factoryTemp`、`productionBatchNo` |
| 3 批发商 | `wholesaleInfo` | `wholesaleDate`、`inboundTemp`、`coldStorageTemp`、`outboundTemp`、`transportToolNo`、`coldStorageNo` |
| 4 零售商 | `retailInfo` | `shelfDate`、`displayTemp`、`saleStore`、`displayEquipNo` |

**捕捞与养殖企业批号详情示例（`fishingInfo` 片段）**

```json
{
  "batchNo": "YY20260901001",
  "productVariety": "带鱼",
  "sourceType": 2,
  "sourceTypeName": "捕捞",
  "batchStatus": 3,
  "batchStatusName": "已确认",
  "handoverTemp": -19.50,
  "fishingInfo": {
    "catchBreedDate": "2026-08-28",
    "certificateType": 2,
    "certificateTypeName": "渔获物上岸证明",
    "certificateNo": "SHZ-2026-0828-0157",
    "departureTemp": -19.50,
    "drugReportNo": "YWJC-2026-0912",
    "fishingLogNo": "YL-2026-0828-017"
  }
}
```

**批发商批号详情示例（`wholesaleInfo` 片段）**

```json
{
  "batchNo": "PF20260910001",
  "productVariety": "带鱼",
  "batchStatus": 3,
  "batchStatusName": "已确认",
  "handoverTemp": -18.50,
  "coldChainOk": true,
  "wholesaleInfo": {
    "wholesaleDate": "2026-09-10",
    "inboundTemp": -18.60,
    "coldStorageTemp": -18.80,
    "outboundTemp": -18.50,
    "transportToolNo": "沪B-LC0231",
    "coldStorageNo": "SH-LL-02"
  }
}
```

**业务规则**

1. 查询他企业的批号返回 `code = 403`。
2. `traceCode` 与 `confirmRequest` 仅在存在时返回，否则为 `null`。
3. 批号详情页同时是下游企业"浏览产品批号详情"页面的数据来源。

### 10.3 新建产品批号

**接口说明**：对应各企业"创建产品批号用例"。接口路径统一，请求体字段随登录企业类型不同而变化；后端从 Token 中解析 `enterpriseType`，按类型做字段校验并写入对应的明细表。

**公共约束**

1. `batchNo` 由企业原有管理系统生成后手工录入，输入框 `blur` 事件调用 10.8 校验唯一性。
2. `enterpriseId`、`enterpriseName`、`enterpriseType` 从 Token 解析，不接受前端传入（传入则忽略）。
3. 新建成功后 `batchStatus = 1`（新建），并记录本环节的交接温度。
4. 温度超限时返回 `code = 2004` 警告；若请求体携带 `forceSubmit = true`（前端二次确认后置为 true），则允许保存并置 `coldChainOk = false`。
5. 可选择在新建时直接发布：捕捞与养殖企业传 `publish = true`，其他类型传 `sendConfirmRequest = true`。

| 项 | 内容 |
| --- | --- |
| 请求方式 | POST |
| 请求路径 | `/api/enterprise/batches` |
| 权限 | 企业 |

**请求参数（Body）——全类型汇总表**

| 参数名 | 类型 | 必填 | 适用企业类型 | 说明 | 示例 |
| --- | --- | --- | --- | --- | --- |
| `batchNo` | String | 是 | 全部 | 产品批号，1~60 字符 | `YY20260901001` |
| `productVariety` | String | 是 | 全部 | 产品品种；类型 2/3/4 由上游带出 | `带鱼` |
| `sourceType` | Integer | 是 | 全部 | 来源类型：1 养殖，2 捕捞；类型 2/3/4 由上游带出 | `2` |
| `publish` | Boolean | 否 | 1 | 是否同时发布，默认 `false` | `true` |
| `sendConfirmRequest` | Boolean | 否 | 2/3/4 | 是否同时向上游发送确认请求，默认 `false` | `true` |
| `forceSubmit` | Boolean | 否 | 全部 | 温度超限时是否强制提交 | `false` |
| `remark` | String | 否 | 全部 | 备注，最长 255 字符 | `2026 年第 17 航次渔获` |
| `catchBreedDate` | String(date) | 是 | 1 | 捕捞或养殖日期 | `2026-08-28` |
| `certificateType` | Integer | 是 | 1 | 证明类型：1 产地检疫合格证明，2 渔获物上岸证明 | `2` |
| `certificateNo` | String | 是 | 1 | 产地检疫合格证明编号或渔获物上岸证明编号 | `SHZ-2026-0828-0157` |
| `departureTemp` | Number | 是 | 1 | 起运温度（℃），要求 ≤ -18 | `-19.5` |
| `drugReportNo` | String | 否 | 1 | 水产品药物残留检测报告编号 | `YWJC-2026-0912` |
| `fishingLogNo` | String | 否 | 1 | 渔捞日志或养殖生产记录编号 | `YL-2026-0828-017` |
| `upstreamEnterpriseId` | String | 是 | 2/3/4 | 上游企业 ID | `1` |
| `upstreamBatchId` | String | 是 | 2/3/4 | 上游产品批号 ID（须为已发布状态） | `1` |
| `processForm` | String | 是 | 2 | 加工形态：整鱼、去头、鱼片、鱼段、鱼块、鱼柳、虾仁等 | `鱼段` |
| `inspectionNo` | String | 是 | 2 | 出厂检验报告编号 | `CYJ-2026-0905-0233` |
| `quickFreezeTemp` | Number | 是 | 2 | 速冻中心温度（℃），要求 ≤ -35 | `-36.2` |
| `factoryTemp` | Number | 是 | 2 | 出厂温度（℃），要求 ≤ -18 | `-19.0` |
| `productionBatchNo` | String | 否 | 2 | 生产批次记录编号 | `SC-2026-0905-014` |
| `wholesaleDate` | String(date) | 是 | 3 | 批发日期 | `2026-09-10` |
| `inboundTemp` | Number | 是 | 3 | 入库温度（℃），要求 ≤ -18 | `-18.6` |
| `coldStorageTemp` | Number | 是 | 3 | 冷库温度（℃），要求 ≤ -18 | `-18.8` |
| `outboundTemp` | Number | 是 | 3 | 出库温度（℃），要求 ≤ -18 | `-18.5` |
| `transportToolNo` | String | 是 | 3 | 运输工具编号（冷藏车/冷藏船） | `沪B-LC0231` |
| `coldStorageNo` | String | 否 | 3 | 冷库编号 | `SH-LL-02` |
| `shelfDate` | String(date) | 是 | 4 | 上架日期 | `2026-09-15` |
| `displayTemp` | Number | 是 | 4 | 冷冻陈列柜温度（℃），要求 ≤ -18 | `-18.2` |
| `saleStore` | String | 是 | 4 | 销售门店 | `佳鲜生鲜超市 陆家嘴店` |
| `displayEquipNo` | String | 否 | 4 | 陈列设备编号 | `LG-03` |

> 说明：加工形态、产品品种的候选值由字典接口 5.3 提供（`process_form`、`product_variety`），业务库中存储中文名称。

**请求示例（类型 1：捕捞与养殖企业）**

```json
{
  "batchNo": "YY20260901001",
  "productVariety": "带鱼",
  "sourceType": 2,
  "catchBreedDate": "2026-08-28",
  "certificateType": 2,
  "certificateNo": "SHZ-2026-0828-0157",
  "departureTemp": -19.50,
  "drugReportNo": "YWJC-2026-0912",
  "fishingLogNo": "YL-2026-0828-017",
  "publish": true,
  "remark": "2026 年第 17 航次渔获"
}
```

**请求示例（类型 2：冷冻加工企业）**

```json
{
  "batchNo": "JG20260905001",
  "upstreamEnterpriseId": "1",
  "upstreamBatchId": "1",
  "processForm": "鱼段",
  "inspectionNo": "CYJ-2026-0905-0233",
  "quickFreezeTemp": -36.20,
  "factoryTemp": -19.00,
  "productionBatchNo": "SC-2026-0905-014",
  "sendConfirmRequest": true
}
```

**请求示例（类型 3：批发商）**

```json
{
  "batchNo": "PF20260910001",
  "upstreamEnterpriseId": "2",
  "upstreamBatchId": "2",
  "wholesaleDate": "2026-09-10",
  "inboundTemp": -18.60,
  "coldStorageTemp": -18.80,
  "outboundTemp": -18.50,
  "transportToolNo": "沪B-LC0231",
  "coldStorageNo": "SH-LL-02",
  "sendConfirmRequest": true
}
```

**请求示例（类型 4：零售商）**

```json
{
  "batchNo": "LS20260915001",
  "upstreamEnterpriseId": "3",
  "upstreamBatchId": "3",
  "shelfDate": "2026-09-15",
  "displayTemp": -18.20,
  "saleStore": "佳鲜生鲜超市 陆家嘴店",
  "displayEquipNo": "LG-03",
  "sendConfirmRequest": true
}
```

> 类型 2/3/4 的 `upstreamEnterpriseId`、`upstreamBatchId` 提交后，后端自动回填上游企业所在省、市、名称与上游批号，并从上游批号继承 `productVariety` 与 `sourceType`。

**响应示例**

```json
{
  "code": 200,
  "message": "新建成功",
  "data": {
    "id": "11",
    "batchNo": "YY20260901001",
    "batchStatus": 3,
    "batchStatusName": "已确认",
    "handoverTemp": -19.50,
    "coldChainOk": true,
    "confirmRequestNo": null,
    "traceCode": null
  }
}
```

`data` 字段说明：

| 字段 | 说明 |
| --- | --- |
| `batchStatus` | 新建后的状态：未发布为 1；`publish = true` 时为 3；`sendConfirmRequest = true` 时为 2 |
| `coldChainOk` | 本环节冷链是否合格 |
| `confirmRequestNo` | 若同时发送了确认请求，返回请求单号 |
| `traceCode` | 仅零售商批号在状态为 3 时返回（新建时通常为 `null`） |

### 10.4 更新产品批号

**接口说明**：对应各企业"更新产品批号用例"及事件描述"更新页面初始化时查询当前产品批号信息并填充到更新表单中；点击更新按钮进行更新操作，并可选择是否发布（或向上游企业发送确认请求）"。

| 项 | 内容 |
| --- | --- |
| 请求方式 | PUT |
| 请求路径 | `/api/enterprise/batches/{id}` |
| 权限 | 企业（只能更新本企业批号） |

**与 10.3 的字段差异**

| 参数名 | 差异 |
| --- | --- |
| `batchNo` | **不可修改**（更新页面批号输入框禁用）。后端收到该字段时忽略，不报错 |
| `publish` | 类型 1 可选；置 `true` 时状态由 1 变为 3 |
| `sendConfirmRequest` | 类型 2/3/4 可选；置 `true` 时状态由 1 变为 2，并生成确认请求 |
| `upstreamEnterpriseId`、`upstreamBatchId` | 类型 2/3/4 允许修改，用于更换上游来源；更换后上游带出字段整体刷新 |
| 其余字段 | 同 10.3，按企业类型必填 |

**请求示例（类型 2：更新后同时向上游发送确认请求）**

```
PUT /api/enterprise/batches/7
```

```json
{
  "upstreamEnterpriseId": "1",
  "upstreamBatchId": "1",
  "processForm": "鱼片",
  "inspectionNo": "CYJ-2026-0920-0301",
  "quickFreezeTemp": -36.50,
  "factoryTemp": -19.20,
  "productionBatchNo": "SC-2026-0920-021",
  "sendConfirmRequest": true
}
```

**响应示例**

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": "7",
    "batchNo": "JG20260920001",
    "batchStatus": 2,
    "batchStatusName": "待确认",
    "handoverTemp": -19.20,
    "coldChainOk": true,
    "confirmRequestNo": "FSTS-CR-20260921-0002",
    "traceCode": null
  }
}
```

**业务规则**

1. 只有 `batchStatus = 1`（新建）的批号允许更新；对"待确认"或"已确认"批号调用本接口返回 `code = 2005`（需求规定待确认状态只能查看、不能修改）。
2. 更新时重新校验温度；`coldChainOk` 与 `handoverTemp` 同步更新。
3. 已发送确认请求的批号被更新后，请求内容同步刷新（请求单号保持不变，`upstream_batch_id`、`handover_temp` 重新计算）。

---

### 10.5 删除产品批号

| 项 | 内容 |
| --- | --- |
| 请求方式 | DELETE |
| 请求路径 | `/api/enterprise/batches/{id}` |
| 权限 | 企业（只能删除本企业批号） |

**响应示例**

```json
{
  "code": 200,
  "message": "删除成功，删除后不可恢复",
  "data": null
}
```

**业务规则**

1. 只有 `batchStatus = 1`（新建）的批号允许删除；其他状态返回 `code = 2005`。
2. 采用物理删除，环节明细表记录由外键 `ON DELETE CASCADE` 同步删除（对应需求"删除后不可恢复"）。
3. 若批号已生成确认请求，则请求记录一并删除（同为级联）。
4. 前端删除前需二次确认。

---

### 10.6 发布产品批号

**接口说明**：捕捞与养殖企业处于链条源头，无上游企业，因此用"发布"代替"向上游发送确认请求"（对应事件描述"更新按钮 Click：如果选中是否发布，那么还要将当前产品批号状态更新为已发布"）。

| 项 | 内容 |
| --- | --- |
| 请求方式 | PUT |
| 请求路径 | `/api/enterprise/batches/{id}/publish` |
| 权限 | 捕捞与养殖企业（类型 1） |
| 请求参数 | 无 |

**响应示例**

```json
{
  "code": 200,
  "message": "发布成功",
  "data": {
    "id": "1",
    "batchNo": "YY20260901001",
    "batchStatus": 3,
    "batchStatusName": "已确认",
    "publishTime": "2026-09-01 08:30:00"
  }
}
```

**业务规则**

1. 只有 `batchStatus = 1` 可发布；发布后状态置 3 并记录 `publish_time`，批号即可被下游冷冻加工企业选择。
2. 类型 2/3/4 调用本接口返回 `code = 400`，提示"请使用向上游企业发送确认请求接口"。

---

### 10.7 下架产品批号

**接口说明**：对应各企业"下架产品批号用例"——"将产品批号信息中的状态属性更新为下架状态"。下架后该批号不可浏览、不可被下游选择。

| 项 | 内容 |
| --- | --- |
| 请求方式 | PUT |
| 请求路径 | `/api/enterprise/batches/{id}/off-shelf` |
| 权限 | 企业（只能下架本企业批号） |
| 请求参数（Body，可选） | `remark`（下架原因，最长 255 字符） |

**请求示例**

```json
{
  "remark": "航次记录更正，原批号停用"
}
```

**响应示例**

```json
{
  "code": 200,
  "message": "下架成功",
  "data": {
    "id": "10",
    "batchNo": "YY20260908001",
    "batchStatus": 4,
    "batchStatusName": "已下架",
    "offShelfTime": "2026-09-18 11:20:00"
  }
}
```

**业务规则**

1. 只有 `batchStatus = 3`（已确认/已发布）允许下架；其他状态返回 `code = 2005`。
2. 下架后：批号不再出现在列表与上游可选中；若该批号为零售商批号，其溯源标识码状态同步置 0，消费者查询时返回 `code = 3002`。
3. 已下架批号的溯源链数据仍保留在数据库中，用于责任追溯与监管核查。

---

### 10.8 产品批号唯一性校验

**接口说明**：用于新建/更新页面中批号输入框的 `blur` 事件（事件描述："产品批号输入框 Blur：检查输入的产品批号是否已经存在"）。校验范围为**当前企业**，不同企业可使用相同的批号字符串。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/batches/check-batch-no` |
| 权限 | 企业 |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `batchNo` | String | 是 | 待校验的产品批号 | `JG20260920001` |
| `excludeId` | String | 否 | 更新场景下排除自身 ID | `7` |

**响应示例**

```json
{
  "code": 200,
  "message": "校验完成",
  "data": { "available": false }
}
```

> `available = false` 时前端提示"该产品批号已存在"，并阻止提交（后端在 10.3/10.4 中同样校验，返回 `code = 2001`）。

---

### 10.9 向上游企业发送确认请求

**接口说明**：对应事件描述"更新按钮 Click：保存更新。若勾选确认请求复选框，则对上游企业生成一条待确认的进场确认请求，并把当前产品批号状态置为待确认"。对类型 2/3/4 而言，本操作等价于该环节的"发布"。

| 项 | 内容 |
| --- | --- |
| 请求方式 | POST |
| 请求路径 | `/api/enterprise/batches/{id}/confirm-request` |
| 权限 | 冷冻加工企业、批发商、零售商（类型 2/3/4） |
| 请求参数 | 无（批号 ID 从路径获取，上游信息取自批号记录） |

**响应示例**

```json
{
  "code": 200,
  "message": "已向上游企业发送进场确认请求",
  "data": {
    "requestId": "3",
    "requestNo": "FSTS-CR-20260919-0001",
    "requestStatus": 1,
    "requestStatusName": "待确认",
    "batchId": "8",
    "batchNo": "PF20260919001",
    "batchStatus": 2,
    "batchStatusName": "待确认",
    "upstreamEnterpriseId": "2",
    "upstreamEnterpriseName": "烟台海珍冷冻食品有限公司",
    "upstreamBatchNo": "JG20260905001",
    "handoverTemp": -18.60,
    "requestTime": "2026-09-19 14:12:00"
  }
}
```

**业务规则**

1. 前置条件：批号属于当前企业、`batchStatus = 1`、已填写上游企业与企业产品批号；否则返回 `code = 400` 或 `code = 2005`。
2. 上游批号必须处于"已确认/已发布"状态，否则返回 `code = 2003`。
3. 请求单号规则：`FSTS-CR-yyyyMMdd-{4 位流水}`。
4. 同一批号重复发送：若已存在"待确认"请求，返回 `code = 409`，提示"该批号已存在待确认的进场确认请求"；若原请求已被拒绝（状态 3），允许重新发送并复用原请求记录。
5. 捕捞与养殖企业调用本接口返回 `code = 400`（源头无上游）。
6. 需求规格说明书中本功能通过更新页面的复选框实现，本文档保留独立接口，同时 10.3/10.4 也支持以 `sendConfirmRequest = true` 的等价方式触发，两种方式效果一致。

---

### 10.10 查询溯源标识码

**接口说明**：对应"获取系统生成的溯源标识码用例"——"产品批号状态变为已确认时，系统自动生成该批次的溯源标识码，零售商可以在详情页查看并展示给消费者"。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/batches/{id}/trace-code` |
| 权限 | 零售商（类型 4） |
| 请求参数 | 无 |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "traceCode": "FSTS-20260915-SH-0001",
    "batchId": "4",
    "batchNo": "LS20260915001",
    "productVariety": "带鱼",
    "saleStore": "佳鲜生鲜超市 陆家嘴店",
    "qrContent": "https://fsts.example.com/trace/FSTS-20260915-SH-0001",
    "status": 1,
    "statusName": "有效",
    "generateTime": "2026-09-15 07:36:00",
    "queryCount": 12,
    "lastQueryTime": "2026-09-20 19:22:00"
  }
}
```

**业务规则**

1. 批号状态未达到"已确认"时返回 `code = 409`，提示"该批号尚未确认，暂无溯源标识码"。
2. 非零售商企业调用返回 `code = 403`。
3. `qrContent` 为二维码内容（追溯页面链接），供后续二维码功能使用；当前版本前端以文本 + 放大弹窗方式展示标识码。

---

## 11 流通节点端——下游企业进场确认

本章对应各企业"下游企业进场确认用例"及事件描述表："初始化时查询下游企业产品批号信息列表，条件是进场产品批号为当前企业的产品批号，并且状态为待确认；点击确认按钮将下游企业产品批号中的状态更新为已确认状态"。

### 11.1 查询待处理进场确认请求列表

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/confirm-requests` |
| 权限 | 企业（接收方为本企业；类型 1/2/3） |

**请求参数（Query）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `requestStatus` | Integer | 否 | 请求状态，默认 1（待确认） | `1` |
| `downstreamEnterpriseName` | String | 否 | 下游企业名称，模糊查询（对应"查找按钮"事件） | `甬海` |
| `batchNo` | String | 否 | 下游产品批号，模糊匹配 | `PF2026` |
| `current` | Long | 否 | 当前页码，默认 1 | `1` |
| `size` | Long | 否 | 每页条数，默认 10 | `10` |

**请求示例**

```
GET /api/enterprise/confirm-requests?requestStatus=1&downstreamEnterpriseName=甬海&current=1&size=10
```

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "records": [
      {
        "id": "2",
        "requestNo": "FSTS-CR-20260919-0001",
        "batchId": "8",
        "batchNo": "PF20260919001",
        "upstreamBatchId": "2",
        "upstreamBatchNo": "JG20260905001",
        "fromEnterpriseId": "3",
        "fromEnterpriseName": "上海甬海水产批发有限公司",
        "fromEnterpriseType": 3,
        "fromEnterpriseTypeName": "批发商",
        "toEnterpriseId": "2",
        "toEnterpriseName": "烟台海珍冷冻食品有限公司",
        "handoverTemp": -18.60,
        "requestStatus": 1,
        "requestStatusName": "待确认",
        "requestTime": "2026-09-19 14:12:00"
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

**业务规则**

1. 只返回 `to_enterprise_id = 当前企业 ID` 的请求，企业之间数据互不可见。
2. 列表默认按 `requestTime` 升序（先到先处理）。
3. 若无待处理请求，`records` 为空数组，前端提示"暂无待确认的进场请求"。

### 11.2 查询确认请求详情

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/enterprise/confirm-requests/{id}` |
| 权限 | 企业（请求的接收方或发起方） |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": "2",
    "requestNo": "FSTS-CR-20260919-0001",
    "requestStatus": 1,
    "requestStatusName": "待确认",
    "requestTime": "2026-09-19 14:12:00",
    "handleTime": null,
    "handleRemark": null,
    "fromEnterpriseName": "上海甬海水产批发有限公司",
    "fromEnterpriseTypeName": "批发商",
    "fromProvinceName": "上海市",
    "fromCityName": "上海市",
    "toEnterpriseName": "烟台海珍冷冻食品有限公司",
    "downstreamBatch": {
      "batchId": "8",
      "batchNo": "PF20260919001",
      "productVariety": "带鱼",
      "wholesaleDate": "2026-09-19",
      "inboundTemp": -18.70,
      "coldStorageTemp": -18.90,
      "outboundTemp": -18.60,
      "handoverTemp": -18.60,
      "coldChainOk": true,
      "saleStore": null
    },
    "upstreamBatch": {
      "batchId": "2",
      "batchNo": "JG20260905001",
      "productVariety": "带鱼",
      "handoverTemp": -19.00,
      "publishTime": "2026-09-05 16:20:00"
    }
  }
}
```

> 上游企业确认前可对照本节数据核对下游登记的品种、数量来源与本企业批号是否一致，其中 `downstreamBatch` 按下游企业类型返回对应的温度字段。

### 11.3 确认进场

| 项 | 内容 |
| --- | --- |
| 请求方式 | PUT |
| 请求路径 | `/api/enterprise/confirm-requests/{id}/confirm` |
| 权限 | 企业（类型 1/2/3，且为请求接收方） |

**请求参数（Body，可选）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `handleRemark` | String | 否 | 处理说明，最长 200 字符 | `实收数量与温度记录相符，同意确认` |

**请求示例**

```json
{
  "handleRemark": "实收数量与温度记录相符，同意确认"
}
```

**响应示例**

```json
{
  "code": 200,
  "message": "确认成功",
  "data": {
    "requestId": "1",
    "requestNo": "FSTS-CR-20260914-0001",
    "requestStatus": 2,
    "requestStatusName": "已确认",
    "handleTime": "2026-09-15 07:35:00",
    "batchId": "4",
    "batchNo": "LS20260915001",
    "batchStatus": 3,
    "batchStatusName": "已确认",
    "traceCode": {
      "traceCode": "FSTS-20260915-SH-0001",
      "generateTime": "2026-09-15 07:36:00",
      "status": 1
    }
  }
}
```

**业务规则（服务端事务内依次完成）**

1. 校验请求存在、`to_enterprise_id = 当前企业 ID`、`requestStatus = 1`；否则返回 `code = 404` 或 `code = 409`。
2. 将 `batch_confirm_request` 的 `request_status` 置 2，写入 `handle_time` 与 `handle_remark`。
3. 将下游产品批号 `batch_status` 置 3，写入 `publish_time`。
4. 若下游批号为零售商批号（`enterprise_type = 4`），自动生成溯源标识码：写入 `trace_code` 表（`trace_code` 全局唯一、`batch_id` 唯一），并在响应中返回；标识码生成失败时整体回滚。
5. 捕捞与养殖企业作为最终确认方，确认后其下游（冷冻加工企业）批号即可被下一环节选择。

### 11.4 拒绝进场（扩展功能）

> 需求规格说明书未强制要求本接口，为保证流程闭环与异常场景可追溯，作为扩展保留。

| 项 | 内容 |
| --- | --- |
| 请求方式 | PUT |
| 请求路径 | `/api/enterprise/confirm-requests/{id}/reject` |
| 权限 | 企业（类型 1/2/3，且为请求接收方） |

**请求参数（Body）**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `handleRemark` | String | 是 | 拒绝原因 | `温度记录与实收批次不符` |

**响应示例**

```json
{
  "code": 200,
  "message": "已拒绝进场确认请求",
  "data": {
    "requestId": "3",
    "requestStatus": 3,
    "requestStatusName": "已拒绝",
    "batchId": "6",
    "batchStatus": 1,
    "batchStatusName": "新建"
  }
}
```

**业务规则**：拒绝后下游批号状态回退为 1（新建），下游企业可修正数据后重新发送确认请求。

---

## 12 消费者端——溯源查询

### 12.1 按溯源标识码查询溯源信息

**接口说明**：对应消费者端"输入溯源标识码获取溯源信息用例"与事件描述表——"根据溯源标识码查询该批次的溯源信息，并按照捕捞与养殖环节、冷冻加工环节、批发与冷链仓储环节、零售环节的顺序展示"。消费者端首页输入标识码后跳转溯源信息页面并调用本接口。

| 项 | 内容 |
| --- | --- |
| 请求方式 | GET |
| 请求路径 | `/api/public/trace/{traceCode}` |
| 权限 | 匿名 |

**路径参数**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `traceCode` | String | 是 | 溯源标识码 | `FSTS-20260915-SH-0001` |

**响应示例**

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "traceCode": "FSTS-20260915-SH-0001",
    "batchNo": "LS20260915001",
    "productVariety": "带鱼",
    "retailerName": "上海佳鲜生鲜超市有限公司",
    "saleStore": "佳鲜生鲜超市 陆家嘴店",
    "generateTime": "2026-09-15 07:36:00",
    "queryCount": 13,
    "coldChainQualified": true,
    "coldChainConclusion": "本批次自捕捞与养殖环节至零售环节共 4 个环节温度记录全部合格，冷链未断链",
    "temperatureCurve": [
      { "stageCode": 1, "stageName": "捕捞与养殖环节", "enterpriseName": "青岛远洋渔业有限公司",
        "temperature": -19.50, "threshold": -18, "qualified": true, "recordTime": "2026-09-01 08:30:00" },
      { "stageCode": 2, "stageName": "冷冻加工环节", "enterpriseName": "烟台海珍冷冻食品有限公司",
        "temperature": -19.00, "threshold": -18, "qualified": true, "recordTime": "2026-09-05 16:20:00" },
      { "stageCode": 3, "stageName": "批发与冷链仓储环节", "enterpriseName": "上海甬海水产批发有限公司",
        "temperature": -18.50, "threshold": -18, "qualified": true, "recordTime": "2026-09-10 09:15:00" },
      { "stageCode": 4, "stageName": "零售环节", "enterpriseName": "上海佳鲜生鲜超市有限公司",
        "temperature": -18.20, "threshold": -18, "qualified": true, "recordTime": "2026-09-15 07:40:00" }
    ],
    "links": [
      {
        "stageCode": 1,
        "stageName": "捕捞与养殖环节",
        "enterpriseId": "1",
        "enterpriseName": "青岛远洋渔业有限公司",
        "enterpriseTypeName": "捕捞与养殖企业",
        "provinceName": "山东省",
        "cityName": "青岛市",
        "batchNo": "YY20260901001",
        "upstreamBatchNo": null,
        "productVariety": "带鱼",
        "sourceTypeName": "捕捞",
        "handoverTemp": -19.50,
        "coldChainOk": true,
        "handoverTime": "2026-09-01 08:30:00",
        "certificates": [
          { "name": "渔获物上岸证明", "no": "SHZ-2026-0828-0157" },
          { "name": "水产品药物残留检测报告", "no": "YWJC-2026-0912" }
        ],
        "temperatures": [
          { "name": "起运温度", "value": -19.50, "threshold": -18, "qualified": true }
        ]
      },
      {
        "stageCode": 2,
        "stageName": "冷冻加工环节",
        "enterpriseId": "2",
        "enterpriseName": "烟台海珍冷冻食品有限公司",
        "enterpriseTypeName": "冷冻加工企业",
        "provinceName": "山东省",
        "cityName": "烟台市",
        "batchNo": "JG20260905001",
        "upstreamBatchNo": "YY20260901001",
        "productVariety": "带鱼",
        "sourceTypeName": "捕捞",
        "handoverTemp": -19.00,
        "coldChainOk": true,
        "handoverTime": "2026-09-05 16:20:00",
        "certificates": [
          { "name": "出厂检验报告", "no": "CYJ-2026-0905-0233" }
        ],
        "temperatures": [
          { "name": "速冻中心温度", "value": -36.20, "threshold": -35, "qualified": true },
          { "name": "出厂温度", "value": -19.00, "threshold": -18, "qualified": true }
        ]
      },
      {
        "stageCode": 3,
        "stageName": "批发与冷链仓储环节",
        "enterpriseId": "3",
        "enterpriseName": "上海甬海水产批发有限公司",
        "enterpriseTypeName": "批发商",
        "provinceName": "上海市",
        "cityName": "上海市",
        "batchNo": "PF20260910001",
        "upstreamBatchNo": "JG20260905001",
        "productVariety": "带鱼",
        "sourceTypeName": "捕捞",
        "handoverTemp": -18.50,
        "coldChainOk": true,
        "handoverTime": "2026-09-10 09:15:00",
        "certificates": [
          { "name": "冷库编号", "no": "SH-LL-02" },
          { "name": "运输工具编号", "no": "沪B-LC0231" }
        ],
        "temperatures": [
          { "name": "入库温度", "value": -18.60, "threshold": -18, "qualified": true },
          { "name": "冷库温度", "value": -18.80, "threshold": -18, "qualified": true },
          { "name": "出库温度", "value": -18.50, "threshold": -18, "qualified": true }
        ]
      },
      {
        "stageCode": 4,
        "stageName": "零售环节",
        "enterpriseId": "4",
        "enterpriseName": "上海佳鲜生鲜超市有限公司",
        "enterpriseTypeName": "零售商",
        "provinceName": "上海市",
        "cityName": "上海市",
        "batchNo": "LS20260915001",
        "upstreamBatchNo": "PF20260910001",
        "productVariety": "带鱼",
        "sourceTypeName": "捕捞",
        "handoverTemp": -18.20,
        "coldChainOk": true,
        "handoverTime": "2026-09-15 07:40:00",
        "certificates": [],
        "temperatures": [
          { "name": "冷冻陈列柜温度", "value": -18.20, "threshold": -18, "qualified": true }
        ]
      }
    ]
  }
}
```

**字段说明**

| 字段 | 说明 |
| --- | --- |
| `traceCode` | 溯源标识码（需求要求展示） |
| `batchNo` | 零售商产品批号 |
| `productVariety` | 产品品种 |
| `coldChainQualified` | 本批次冷链温度记录是否合格的结论（四个环节全部合格才为 `true`） |
| `coldChainConclusion` | 结论文案，直接展示在页面上 |
| `temperatureCurve` | 温度曲线图数据源（ECharts 折线图，横轴为环节，纵轴为温度，附合格阈值线 -18 ℃） |
| `links` | 按环节顺序排列的溯源链，每条对应一个流通环节 |
| `certificates` | 该环节关键凭证与过程记录编号（无编号时为空数组） |
| `temperatures` | 该环节登记的全部温度明细 |
| `handoverTime` | 本环节交接时间，取该批号的发布/确认时间（`publish_time`） |

> 说明：批发环节按《需求陈述书》表 1-1"产品凭证与过程记录"的约定，无编号式凭证，因此在 `certificates` 中返回冷库编号与运输工具编号，用于定位本批次的仓储与承运资源。

**业务规则**

1. 从零售商批号出发，沿 `upstream_batch_id` 逐级向上追溯至源头，再按 `enterprise_type` 升序（1→2→3→4）返回 `links`，保证环节顺序符合需求。
2. 不返回企业联系人、联系电话、账号等敏感信息，仅返回企业名称、所在省市。
3. 每次查询成功后将 `trace_code.query_count` 加 1 并更新 `last_query_time`。
4. 标识码不存在返回 `code = 3001`；标识码状态为 0（批号已下架）返回 `code = 3002`。
5. 若某环节 `coldChainOk = 0`，则 `coldChainQualified = false`，结论文案提示"环节X温度记录不合格，存在冷链断链风险"，并可在页面中以红色高亮该环节。

---

## 附录 A 前端页面与接口对照表

### A.1 系统管理端

| 页面 | 路由建议 | 功能点 | 调用接口 |
| --- | --- | --- | --- |
| 登录页 | `/admin/login` | 管理员登录 | 4.1 |
| 首页 / 可视化大屏 | `/admin/dashboard` | 数据卡片、折线图、饼图、柱状图 | 7.1、7.2、7.3、7.4、7.5 |
| 节点企业注册信息管理 | `/admin/enterprises` | 列表初始化、分页、模糊查询、清空、详情、编辑、删除 | 6.1、6.2、6.4、6.5、5.1、5.2 |
| 节点企业新建对话框 | 同上页面弹窗 | 企业类型切换资质字段、新建 | 5.1、5.2、6.3、6.6 |

### A.2 流通节点端

| 页面 | 路由建议 | 功能点 | 调用接口 |
| --- | --- | --- | --- |
| 登录页 | `/enterprise/login` | 企业登录 | 4.4 |
| 首页 | `/enterprise/home` | 当前企业概览 | 4.5 |
| 企业注册信息管理 | `/enterprise/profile` | 查看本企业注册信息、修改密码 | 8.1、4.6 |
| 产品批号列表 | `/enterprise/batches` | 按状态分组查询、更新、删除、下架 | 10.1、10.5、10.7 |
| 新建产品批号 | `/enterprise/batches/create` | 按企业类型渲染表单、批号校验、上游选择 | 5.1、5.2、5.3、9.1、9.2、10.8、10.3 |
| 更新产品批号 | `/enterprise/batches/edit/:id` | 回显、更新、发布或发送确认请求 | 10.2、10.4、10.6、10.9 |
| 产品批号详情 | `/enterprise/batches/detail/:id` | 查看批号详情与确认状态 | 10.2、10.10（零售商） |
| 下游企业进场确认 | `/enterprise/confirm-requests` | 列表、查找、确认 | 11.1、11.2、11.3 |

### A.3 消费者端

| 页面 | 路由建议 | 功能点 | 调用接口 |
| --- | --- | --- | --- |
| 溯源首页 | `/trace` | 输入溯源标识码、格式校验、跳转 | —— |
| 溯源结果页 | `/trace/:traceCode` | 按环节展示溯源信息、温度曲线、冷链结论 | 12.1 |

---

## 附录 B 前端 Axios 封装示例

### B.1 request.js（请求实例与拦截器）

```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '@/router'

const request = axios.create({
  baseURL: '/api',
  timeout: 10000,
  headers: { 'Content-Type': 'application/json;charset=UTF-8' }
})

// 请求拦截器：注入 Token
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers['Authorization'] = 'Bearer ' + token
  }
  return config
})

// 响应拦截器：统一处理业务码
request.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code === 200) {
      return res.data
    }
    if (res.code === 401) {
      localStorage.removeItem('token')
      router.push('/login')
    }
    ElMessage.error(res.message || '操作失败')
    return Promise.reject(res)
  },
  error => {
    if (error.response && error.response.status === 401) {
      localStorage.removeItem('token')
      router.push('/login')
    }
    ElMessage.error('网络异常，请稍后重试')
    return Promise.reject(error)
  }
)

export default request
```

### B.2 api 模块划分建议

```
src/api/
  auth.js        // 4.1 - 4.7
  common.js      // 5.1 - 5.3
  adminEnterprise.js  // 6.x
  adminStatistics.js  // 7.x
  enterpriseBatch.js  // 8.x - 10.x
  confirmRequest.js   // 11.x
  trace.js            // 12.1（消费者端，无需 Token）
```

```javascript
// src/api/enterpriseBatch.js
import request from '@/utils/request'

export const pageBatch = params => request({ url: '/enterprise/batches', method: 'get', params })
export const getBatch = id => request({ url: `/enterprise/batches/${id}`, method: 'get' })
export const createBatch = data => request({ url: '/enterprise/batches', method: 'post', data })
export const updateBatch = (id, data) => request({ url: `/enterprise/batches/${id}`, method: 'put', data })
export const deleteBatch = id => request({ url: `/enterprise/batches/${id}`, method: 'delete' })
export const publishBatch = id => request({ url: `/enterprise/batches/${id}/publish`, method: 'put' })
export const offShelfBatch = (id, data) => request({ url: `/enterprise/batches/${id}/off-shelf`, method: 'put', data })
export const checkBatchNo = params => request({ url: '/enterprise/batches/check-batch-no', method: 'get', params })
export const sendConfirmRequest = id => request({ url: `/enterprise/batches/${id}/confirm-request`, method: 'post' })
export const getTraceCode = id => request({ url: `/enterprise/batches/${id}/trace-code`, method: 'get' })
```

---

## 附录 C 统计接口 SQL 参考

以下语句可直接用于 MyBatis Mapper，与建表脚本中提供的视图等价，二者任选其一。

### C.1 概览数据（对应 7.1）

```sql
SELECT COUNT(*) AS total_count,
       SUM(enterprise_type = 1) AS fishing_count,
       SUM(enterprise_type = 2) AS processing_count,
       SUM(enterprise_type = 3) AS wholesale_count,
       SUM(enterprise_type = 4) AS retail_count,
       COUNT(DISTINCT province_code) AS province_count,
       SUM(DATE(register_time) = CURDATE()) AS today_register_count
FROM node_enterprise
WHERE deleted = 0;
```

### C.2 十二个月注册趋势（对应 7.2）

```sql
SELECT DATE_FORMAT(register_time, '%Y-%m') AS month,
       COUNT(*) AS cnt
FROM node_enterprise
WHERE deleted = 0
  AND register_time >= DATE_SUB(DATE_FORMAT(CURDATE(), '%Y-%m-01'), INTERVAL 11 MONTH)
GROUP BY month
ORDER BY month;
```

> 服务层需按当前月向前推 12 个月生成横轴 `months`，再与查询结果左连接补 0，保证 12 个数据点连续。

### C.3 省分组分布（对应 7.3 / 7.4）

```sql
SELECT province_code,
       province_name,
       COUNT(*) AS cnt
FROM node_enterprise
WHERE deleted = 0
GROUP BY province_code, province_name
ORDER BY cnt DESC, province_code ASC;
```

### C.4 企业类型分布（对应 7.5）

```sql
SELECT e.enterprise_type,
       d.item_value AS enterprise_type_name,
       COUNT(*) AS cnt
FROM node_enterprise e
LEFT JOIN sys_dict_item d
       ON d.type_code = 'enterprise_type'
      AND d.item_code = CAST(e.enterprise_type AS CHAR)
WHERE e.deleted = 0
GROUP BY e.enterprise_type, d.item_value
ORDER BY e.enterprise_type;
```

### C.5 溯源链查询（对应 12.1 的核心查询）

```sql
-- 第一步：由溯源标识码定位零售商批号
SELECT t.batch_id
FROM trace_code t
WHERE t.trace_code = 'FSTS-20260915-SH-0001'
  AND t.status = 1;

-- 第二步：使用 MySQL 8.0 递归 CTE 向上追溯整条链路（源头到零售共 4 层）
WITH RECURSIVE chain AS (
    SELECT b.*, 1 AS depth
    FROM product_batch b
    WHERE b.id = 4              -- 零售商批号 ID
      AND b.deleted = 0
    UNION ALL
    SELECT p.*, c.depth + 1
    FROM product_batch p
    JOIN chain c ON p.id = c.upstream_batch_id
    WHERE p.deleted = 0
)
SELECT *
FROM chain
ORDER BY enterprise_type ASC;   -- 1 捕捞与养殖 -> 2 冷冻加工 -> 3 批发 -> 4 零售
```

> 环节明细（`batch_fishing`、`batch_processing`、`batch_wholesale`、`batch_retail`）按 `batch_id` 二次查询后组装，或使用 `LEFT JOIN` 一次性取出。

---

## 附录 D 后端实现要点

### D.1 建议的包结构

```
com.fsts.trace
  ├── common          // Result、PageResult、BusinessException、ErrorCode、JwtUtil、常量
  ├── config          // MybatisPlusConfig、WebMvcConfig（CORS）、JacksonConfig（Long 转 String）
  ├── interceptor     // AuthInterceptor（区分 /admin、/enterprise、/public）
  ├── controller
  │     ├── AdminAuthController         // 4.1 - 4.3
  │     ├── AdminEnterpriseController   // 6.1 - 6.6
  │     ├── AdminStatisticsController   // 7.1 - 7.5
  │     ├── CommonController            // 5.1 - 5.3
  │     ├── EnterpriseAuthController    // 4.4 - 4.7、8.1
  │     ├── EnterpriseBatchController   // 9.1 - 10.10
  │     ├── ConfirmRequestController    // 11.1 - 11.4
  │     └── PublicTraceController       // 12.1
  ├── service / service.impl
  ├── mapper
  ├── entity          // 与数据表一一对应
  └── dto / vo        // 请求 DTO、响应 VO（含 BatchDetailVO、TraceVO 等）
```

### D.2 关键业务规则实现要点

| 规则 | 实现位置 | 要点 |
| --- | --- | --- |
| 批号状态机 | `BatchStatusService` | 统一提供 `assertCanUpdate/assertCanPublish/assertCanOffShelf/assertCanDelete`，避免各接口重复判断；非法流转抛 `BusinessException(2005)` |
| 按企业类型路由 | `EnterpriseBatchService` | 依据 Token 中的 `enterpriseType` 选择 DTO 校验器（`BatchValidator` 策略接口 + 四个实现）与明细表写入逻辑 |
| 上游带出 | `UpstreamService` | 保存前查询上游批号（必须是状态 3），回填 `product_variety`、`source_type`、上游企业省市名称与上游批号 |
| 冷链判定 | `ColdChainService` | 按附录 F 的阈值判定，任意一项超限即 `coldChainOk = 0`；`handover_temp` 按环节取代表温度 |
| 确认请求 | `ConfirmRequestService` | 与批号状态在同一事务内更新；确认时若下游为零售商则调用溯源码生成 |
| 溯源标识码生成 | `TraceCodeService` | 编码 `FSTS-yyyyMMdd-{省简称缩写}-{当日 4 位流水}`，使用数据库唯一索引兜底，冲突时重试；`batch_id` 唯一保证一批一码 |
| 溯源链组装 | `TraceChainService` | 递归向上追溯（见附录 C.5），再按 `enterprise_type` 升序输出 `links` 与 `temperatureCurve` |
| 数据隔离 | MyBatis-Plus 拦截器或 Service 层强制条件 | 企业端所有查询自动追加 `enterprise_id = 当前企业 ID` |

### D.3 统一异常与响应

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusiness(BusinessException e) {
        return Result.fail(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValid(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldErrors().stream()
                .map(FieldError::getDefaultMessage).findFirst().orElse("参数校验失败");
        return Result.fail(400, msg);
    }
}
```

---

## 附录 E 接口字段与数据库表对照

| 接口字段 | 数据库表.字段 | 说明 |
| --- | --- | --- |
| `enterpriseId` | `node_enterprise.id` | 企业主键，序列化为字符串 |
| `enterpriseType` | `node_enterprise.enterprise_type` | 企业类型 |
| `creditCode` | `node_enterprise.credit_code` | 统一社会信用代码 |
| `batchNo` | `product_batch.batch_no` | 产品批号 |
| `upstreamBatchNo` | `product_batch.upstream_batch_no` | 上游（进场）产品批号 |
| `productVariety` | `product_batch.product_variety` | 产品品种 |
| `batchStatus` | `product_batch.batch_status` | 批号状态 |
| `handoverTemp` | `product_batch.handover_temp` | 本环节交接温度 |
| `coldChainOk` | `product_batch.cold_chain_ok` | 本环节冷链是否合格 |
| `catchBreedDate`、`certificateNo` | `batch_fishing.catch_breed_date`、`certificate_no` | 捕捞与养殖环节 |
| `processForm`、`quickFreezeTemp` | `batch_processing.process_form`、`quick_freeze_temp` | 冷冻加工环节 |
| `inboundTemp`、`coldStorageTemp`、`outboundTemp` | `batch_wholesale.inbound_temp`、`cold_storage_temp`、`outbound_temp` | 批发与冷链储运环节 |
| `shelfDate`、`displayTemp`、`saleStore` | `batch_retail.shelf_date`、`display_temp`、`sale_store` | 零售环节 |
| `requestNo`、`requestStatus` | `batch_confirm_request.request_no`、`request_status` | 进场确认请求 |
| `traceCode` | `trace_code.trace_code` | 溯源标识码 |

---

## 附录 F 温度判定规则

依据《01 需求陈述书》表 1-2 与 GB 31605-2020、GB 2733-2015：

| 流通环节 | 温度项 | 判定阈值 | 说明 |
| --- | --- | --- | --- |
| 源头出场（捕捞与养殖企业） | 起运温度 | ≤ -18 ℃ | 起运前中心温度预冷至 -18 ℃ 以下 |
| 冷冻加工（冷冻加工企业） | 速冻中心温度 | ≤ -35 ℃ | 速冻工序要求 |
| 冷冻加工（冷冻加工企业） | 出厂温度 | ≤ -18 ℃ | 成品中心温度要求 |
| 批发与冷链储运（批发商） | 入库温度 | ≤ -18 ℃ | 入库交接 |
| 批发与冷链储运（批发商） | 冷库温度 | ≤ -18 ℃ | 冷库贮存 |
| 批发与冷链储运（批发商） | 出库温度 | ≤ -18 ℃ | 出库交接 |
| 零售（零售商） | 冷冻陈列柜温度 | ≤ -18 ℃ | 终端陈列 |

判定规则：

1. 单个环节内所有温度项均达标时，`product_batch.cold_chain_ok = 1`；任一项超限即为 0。
2. `product_batch.handover_temp` 取该环节的代表温度：捕捞环节取起运温度、加工环节取出厂温度、批发环节取出库温度、零售环节取陈列柜温度，用于消费者端温度曲线。
3. 全链条所有环节 `cold_chain_ok = 1` 时，消费者端 `coldChainQualified = true`。
4. 前端在温度输入框 `blur` 事件中即时比对阈值，超限时给出"冷链异常"提示（对应事件描述"校验输入的入库温度是否高于 -18 ℃，高于 -18 ℃ 时给出冷链异常提示"），提交时返回 `code = 2004` 要求二次确认。

---

## 附录 G 联调验证清单

以下清单基于《冷冻海产品溯源系统-演示数据脚本.sql》，可直接作为前后端联调与验收测试用例。

| 序号 | 用例 | 使用账号 / 数据 | 预期结果 |
| --- | --- | --- | --- |
| 1 | 管理员登录 | `admin` / `123456` | 登录成功，返回 Token 与管理员信息 |
| 2 | 大屏统计 | 登录管理端进入大屏 | 企业总数 16，类型分布 5/3/5/3，覆盖 8 个省，趋势图 12 个月连续无断点 |
| 3 | 企业模糊查询 | 管理端企业列表输入"水产" | 返回名称含"水产"的企业，分页每页 10 条 |
| 4 | 企业新建 | 管理端新建零售商 | 生成 `FSTS-E-...` 编码，返回初始密码；重复执照编号时报错 2002 |
| 5 | 捕捞企业批号列表 | `qd_yuanye` / `123456` | 待发布（新建）分组列表为空；已发布分组显示批号 `YY20260901001`，行内只有"下架"按钮 |
| 6 | 下游选择上游批号 | `yt_haizhen` 新建批号页 | 选择山东省→烟台市→青岛远洋渔业有限公司后，批号下拉只出现 `YY20260901001`，品种自动填充"带鱼"且输入框禁用 |
| 7 | 批号唯一性校验 | `yt_haizhen` 输入 `JG20260905001` | 返回 `available = false`，提示批号已存在 |
| 8 | 重复发送确认请求 | `sh_yonghai` 对已处于待确认状态的 `PF20260919001` 再次发送确认请求 | 返回 `code = 409`，提示该批号已存在待确认的进场确认请求 |
| 9 | 进场确认 | `yt_haizhen` 进入下游企业进场确认页 | 列表出现批发商"上海甬海水产批发有限公司"的待确认请求（请求单号 `FSTS-CR-20260919-0001`），确认后批号 `PF20260919001` 状态变为"已确认" |
| 10 | 零售商获取溯源标识码 | `sh_jiaxian` 查看批号 `LS20260915001` 详情 | 返回标识码 `FSTS-20260915-SH-0001`，状态有效 |
| 11 | 消费者溯源查询 | 消费者端输入 `FSTS-20260915-SH-0001` | 返回 4 个环节，顺序为捕捞与养殖→冷冻加工→批发与冷链仓储→零售，冷链结论为合格 |
| 12 | 冷链异常提示 | `nb_yongjiang` 新建批号时输入出厂温度 -16.5 ℃ | 前端提示冷链异常，后端返回 `code = 2004` |
| 13 | 错误标识码 | 消费者端输入 `FSTS-0000` | 返回 `code = 3001`（标识码不存在） |
| 14 | 数据隔离 | `sh_yonghai` 请求烟台海珍的批号详情 | 返回 `code = 403` 或 404，不能查看他企业批号 |

---

## 附录 H 文档与需求条目对照

| 需求来源 | 对应章节 |
| --- | --- |
| 项目任务书 1 系统管理端（登录、节点企业注册、信息管理、数据统计） | 4.1~4.3、6.1~6.6、7.1~7.5 |
| 项目任务书 2 流通节点端（批号新建/管理/发布下架、确认请求、冷链温度、溯源码） | 8.1、9.1~9.2、10.1~10.10、11.1~11.4 |
| 项目任务书 3 消费者端（按标识码获取溯源信息） | 12.1 |
| 需求规格说明书 4.1 事件描述（初始化、清空、查询、分页、新建、详情、编辑、删除、省市级联、企业类型切换） | 5.1、5.2、6.1~6.6 |
| 需求规格说明书 5.2.1~5.2.7（捕捞与养殖企业各用例） | 10.1~10.8、11.1~11.3 |
| 需求规格说明书 5.3.7、5.4.7、5.5.7（向上游发送确认请求） | 10.9 |
| 需求规格说明书 5.5.8（获取溯源标识码） | 10.10、12.1 |
| 需求规格说明书 7 附录 表 7-1（各环节数据项） | 10.3、附录 E |
| 需求陈述书 表 1-2（冷链温控要求） | 附录 F |
| 需求陈述书 表 1-3（开发环境） | 2.1、附录 B、附录 D |

---

**文档结束**
